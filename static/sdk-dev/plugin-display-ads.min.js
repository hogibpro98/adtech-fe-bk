/*! For license information please see plugin-display-ads.min.js.LICENSE.txt */
(()=>{var __webpack_modules__={"./assets/js/axios-config.js":(module,__unused_webpack_exports,__webpack_require__)=>{eval('const axios = __webpack_require__(/*! axios */ "./node_modules/axios/dist/browser/axios.cjs");\r\nconst _va_constant = __webpack_require__(/*! ./constant */ "./assets/js/constant.js");\r\n\r\nconst _instanceAxiosDisplayAdsCore = axios.create({\r\n  baseURL: _va_constant.SERVER_CONFIG.CORE_API,\r\n  timeout: 30000,\r\n});\r\n\r\nconst _instanceAxiosDisplayAdsLogs = axios.create({\r\n  baseURL: _va_constant.SERVER_CONFIG.LOGS_API,\r\n  timeout: 30000,\r\n});\r\n\r\nmodule.exports = {\r\n  _instanceAxiosDisplayAdsCore,\r\n  _instanceAxiosDisplayAdsLogs\r\n}\r\n\n\n//# sourceURL=webpack:///./assets/js/axios-config.js?')},"./assets/js/constant.js":module=>{eval("const _va_constant = {\r\n  DISPLAY_ADS: {\r\n    CAMPAIGN_ID: 'campaign_id',\r\n    BANNER_ID: 'banner_id',\r\n    ZONE_ID: 'zone_id',\r\n    ADS_EL_ID: 'ads_el_id',\r\n\r\n    VIEWPORT_STATUS: 'viewport_status',\r\n    ADS_VIEW_STATUS: 'ads_view_status',\r\n    ADS_TRIGGER_TIME_STATUS: 'ads_trigger_time_status',\r\n    MEDIA_THRESHOLD_STATUS: 'media_threshold_status',\r\n    MEDIA_STATUS: 'media_status',\r\n    VA_ID: '_na',\r\n  },\r\n  DISPLAY_TYPE: {\r\n    BANNER: 'banner',\r\n    STICKY: 'sticky',\r\n    POPUP: 'popup'\r\n  },\r\n  ADS_VIEWPORT_STATUS: {\r\n    VIEWING: 'viewing',\r\n    NOT_VIEW: 'not_view',\r\n  },\r\n  ADS_VIEW_STATUS: {\r\n    NOT_VIEW: 'not_view',\r\n    VIEWED: 'viewed',\r\n  },\r\n  ADS_TRIGGER_TIME_STATUS: {\r\n    SUCCESS: 'success',\r\n  },\r\n  ACTION_TYPES_DISPLAY_ADS: {\r\n    PAGE_VIEW: 'page_view',\r\n    LOAD_ZONE: 'load_zone',\r\n    BANNER_VIEW: 'banner_view',\r\n    BANNER_CLICK: 'banner_click',\r\n    HEART_BIT: 'heartbit',\r\n    SKIP: 'skip',\r\n    TRIGGER_TIME: 'trigger_time',\r\n  },\r\n  POST_MESSAGE: {\r\n    SOURCE: 'display-ads',\r\n\r\n    EVENT_FULL_SCREEN_ADS: 'event_full_screen_ads',\r\n    EVENT_STICKY_ADS: 'event_sticky_ads',\r\n    EVENT_RESIZE_ZONE: 'event_resize_zone',\r\n    EVENT_PUSH_ADS: 'event_push_ads',\r\n    EVENT_CLOSE_ADS: 'event_close_ads',\r\n    EVENT_VIEW_ADS: 'event_view_ads',\r\n    EVENT_END_VIDEO_ADS: 'event_end_video_ads',\r\n    EVENT_CLICK_ADS: 'event_click_ads',\r\n    EVENT_SKIP_ADS: 'event_skip_ads',\r\n    EVENT_SHOW_BUTTON_SKIP_ADS: 'event_show_button_skip_ads',\r\n    EVENT_HEART_BIT_ADS: 'event_heart_bit_ads',\r\n    EVENT_TRIGGER_TIME_ADS: 'event_trigger_time_ads',\r\n  },\r\n  SERVER_CONFIG: {\r\n    MOCK_API: false,\r\n    CORE_API: \"https://adsdistributor.mhsolution.vn\",\r\n    LOGS_API: \"https://adslogcapture.mhsolution.vn\",\r\n    CLIENT_DOMAIN: \"http://localhost:8080\",\r\n    CLIENT_URL: \"http://localhost:3000/sdk-dev/index.html\",\r\n  },\r\n  ADS_CONFIG: {\r\n    TIME_ACCEPT_VIEWPORT: 3000,\r\n    THRESHOLD_VIEWPORT: 0.7,\r\n    TIME_INTERVAL_MEDIA: 500,\r\n  },\r\n  ZONE_DISPLAY_ADS_CONFIG: {\r\n  },\r\n  DATA_MOCK: {\r\n    ZONE_DISPLAY: {}\r\n  },\r\n  MEDIA_TYPES: {\r\n    IMAGE: 'image',\r\n    VIDEO: 'video',\r\n    AUDIO: 'audio',\r\n    TEXT: 'text',\r\n    IFRAME: 'iframe'\r\n  },\r\n  MEDIA_THRESHOLD: [\r\n    'video',\r\n  ],\r\n  MEDIA_AUDIO: [\r\n    'audio',\r\n    'video',\r\n  ],\r\n  MEDIA_STATUS: {\r\n    ENDED: 'ended'\r\n  },\r\n  VIDEO_STATUS: {\r\n    VIDEO_PLAY: 'play',\r\n    VIDEO_PAUSE: 'pause',\r\n    VIDEO_ENDED: 'ended',\r\n  },\r\n  ATTRIBUTE_INS: {\r\n    STATUS: 'data-adsbyvda-status',\r\n    CLASS_NAME: 'adsbyvda',\r\n    DATA_AD_FORMAT: 'data-ad-format',\r\n    DATA_AD_LAYOUT_KEY: 'data-ad-layout-key',\r\n    DATA_AD_CLIENT: 'data-ad-client',\r\n    DATA_AD_SLOT: 'data-ad-slot',\r\n    DATA_FULL_WIDTH_RESPONSE: 'data-full-width-responsive',\r\n  },\r\n  PARAM_URL_IFRAME: {\r\n    DATA_AD_FORMAT: 'data-ad-format',\r\n    DATA_AD_LAYOUT_KEY: 'data-ad-layout-key',\r\n    DATA_AD_CLIENT: 'data-ad-client',\r\n    DATA_AD_SLOT: 'data-ad-slot',\r\n    DATA_FULL_WIDTH_RESPONSE: 'data-full-width-responsive',\r\n    ADS_EL_ID: 'ads_el_id',\r\n    ZONE_ID: 'zone_id',\r\n    CLIENT_ID: 'client_id',\r\n  },\r\n  ADS_BY_VDS_STATUS: {\r\n    LOAD_ZONE: 'load_zone',\r\n    FILLED: 'filled',\r\n    EMPTY: 'empty',\r\n    ERROR: 'error',\r\n  },\r\n  HEADER_SCRIPT: {\r\n    AD_CLIENT: \"data-ad-client\",\r\n    CHECKED_HEAD: \"data-checked-head\",\r\n    PATHNAME_PLUGIN_SCRIPT: \"/sdk-display-ads.min.js\"\r\n  },\r\n  KEY_MESSAGE_MERGE: \".\",\r\n  VOLUME_STATUS: {\r\n    MUTE: 'mute',\r\n    LISTING: 'listing'\r\n  },\r\n  VOLUME_BUTTON_ATTRS: {\r\n    DATA_TARGET: 'data-target',\r\n    DATA_STATUS: 'data-status',\r\n  },\r\n  REGEX_MEDIA: {\r\n    URL_IMAGE_REGEX: /(http(s?):)([\\/|.|\\w|\\s|\\-|:])*\\.(?:jpg|jpeg|png|gif|webp|psd|raw)/i,\r\n    URL_VIDEO_REGEX: /(http(s?):)([\\/|.|\\w|\\s|\\-|:])*\\.(?:mp4|m4p|m4v|3gp|avi|webm|mov)/i,\r\n    URL_AUDIO_REGEX: /(http(s?):)([\\/|.|\\w|\\s|\\-|:])*\\.(?:mp3|wma|wav|flac|aac|ogg|aif)/i\r\n  },\r\n  ZONE_LAYOUT: {\r\n    MEDIA_ABOVE: 'layout_img_above',\r\n    MEDIA_SIDE: 'layout_img_side',\r\n    TITLE_TOP: 'layout_title_top',\r\n    TEXT_ONLY: 'layout_text_only',\r\n    MEDIA_ONLY: 'layout_img_only',\r\n  },\r\n  UNIT_PROPERTY_SUFFIX: ['padding', 'font-size', 'margin', 'width', 'height', 'max-width', 'max-height', 'border-width', 'top', 'left', 'bottom', 'right'],\r\n  PROPERTY_SUFFIX_DEFAULT: 'px',\r\n}\r\n\r\nmodule.exports = _va_constant;\r\n\n\n//# sourceURL=webpack:///./assets/js/constant.js?")},"./assets/js/icons.js":module=>{eval('const _va_icons = {\r\n  VOLUME_LISTING: \'<svg aria-hidden="true" focusable="false" data-prefix="fas" data-icon="volume" role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 480 512" class="svg-inline--fa fa-volume fa-w-15 fa-3x"><path fill="currentColor" d="M215.03 71.05L126.06 160H24c-13.26 0-24 10.74-24 24v144c0 13.25 10.74 24 24 24h102.06l88.97 88.95c15.03 15.03 40.97 4.47 40.97-16.97V88.02c0-21.46-25.96-31.98-40.97-16.97zM480 256c0-63.53-32.06-121.94-85.77-156.24-11.19-7.14-26.03-3.82-33.12 7.46s-3.78 26.21 7.41 33.36C408.27 165.97 432 209.11 432 256s-23.73 90.03-63.48 115.42c-11.19 7.14-14.5 22.07-7.41 33.36 6.51 10.36 21.12 15.14 33.12 7.46C447.94 377.94 480 319.53 480 256zm-141.77-76.87c-11.58-6.33-26.19-2.16-32.61 9.45-6.39 11.61-2.16 26.2 9.45 32.61C327.98 228.28 336 241.63 336 256c0 14.38-8.02 27.72-20.92 34.81-11.61 6.41-15.84 21-9.45 32.61 6.43 11.66 21.05 15.8 32.61 9.45 28.23-15.55 45.77-45 45.77-76.88s-17.54-61.32-45.78-76.86z" class=""></path></svg>\',\r\n  VOLUME_MUTE: \'<svg aria-hidden="true" focusable="false" data-prefix="fas" data-icon="volume-mute" role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" class="svg-inline--fa fa-volume-mute fa-w-16 fa-3x"><path fill="currentColor" d="M215.03 71.05L126.06 160H24c-13.26 0-24 10.74-24 24v144c0 13.25 10.74 24 24 24h102.06l88.97 88.95c15.03 15.03 40.97 4.47 40.97-16.97V88.02c0-21.46-25.96-31.98-40.97-16.97zM461.64 256l45.64-45.64c6.3-6.3 6.3-16.52 0-22.82l-22.82-22.82c-6.3-6.3-16.52-6.3-22.82 0L416 210.36l-45.64-45.64c-6.3-6.3-16.52-6.3-22.82 0l-22.82 22.82c-6.3 6.3-6.3 16.52 0 22.82L370.36 256l-45.63 45.63c-6.3 6.3-6.3 16.52 0 22.82l22.82 22.82c6.3 6.3 16.52 6.3 22.82 0L416 301.64l45.64 45.64c6.3 6.3 16.52 6.3 22.82 0l22.82-22.82c6.3-6.3 6.3-16.52 0-22.82L461.64 256z" class=""></path></svg>\',\r\n}\r\n\r\nmodule.exports = _va_icons;\n\n//# sourceURL=webpack:///./assets/js/icons.js?')},"./assets/js/management-display-ads.js":(module,__unused_webpack_exports,__webpack_require__)=>{eval('const _va_constant = __webpack_require__(/*! ./constant */ "./assets/js/constant.js");\r\nconst _va_icons = __webpack_require__(/*! ./icons */ "./assets/js/icons.js");\r\nconst _va_typeof = __webpack_require__(/*! ./typeof */ "./assets/js/typeof.js");\r\nconst { getVisibleByEvent, wrap, addListener, createElementFromHTML, convertObjectToStyle, thresholdArray, removeEventVisibleWindow, addEventVisibleWindow, postDataToParent, getScale, resizeByScale } = __webpack_require__(/*! ./util */ "./assets/js/util.js");\r\n\r\nconst managementDisplayAds = {\r\n  // https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API/Timing_element_visibility\r\n  contentBox: null,\r\n  visibleAds: {},\r\n  isVisibleWindow: true,\r\n  adObserver: null,\r\n  timeoutIntersectionObserver: {},\r\n  timeoutTriggerTime: {},\r\n  intervalPlayMedia: {},\r\n\r\n  idDisplayAds: null,\r\n  banners: {},\r\n  controlVideo: {},\r\n  zoneConfig: null,\r\n  zoneId: null,\r\n  dataFullWidthResponsive: false,\r\n  numBannerShow: 0,\r\n  init: function ({ idDisplayAds, zone, banners, dataFullWidthResponsive } = {}) {\r\n    this.handleBindThis();\r\n\r\n    if (!zone) {\r\n      console.error(\'zone is not found\');\r\n      return;\r\n    }\r\n\r\n    if (!idDisplayAds) {\r\n      console.error(\'idDisplayAds is require\');\r\n      return;\r\n    }\r\n\r\n    if (!banners) {\r\n      console.error(\'banners is require\');\r\n      return;\r\n    }\r\n\r\n    const zoneConfig = zone || {};\r\n\r\n    if (!zoneConfig) {\r\n      console.error(\'zone id not found\');\r\n      return;\r\n    }\r\n\r\n    new ResizeObserver(() => {\r\n      const el = document.getElementById(\'show-display-ads\');\r\n      if (el.offsetHeight !== 0) {\r\n        this.resizeRoot(el.offsetWidth, el.offsetHeight);\r\n      } else if (this.numBannerShow === 0) {\r\n        this.resizeRoot(0, 0);\r\n      }\r\n    }).observe(document.getElementById(\'show-display-ads\'))\r\n\r\n    this.idDisplayAds = idDisplayAds;\r\n    this.zoneConfig = zoneConfig;\r\n    this.banners = banners;\r\n    this.dataFullWidthResponsive = dataFullWidthResponsive;\r\n\r\n    this.zoneId = zoneConfig.id;\r\n    this.contentBox = document.querySelector(idDisplayAds);\r\n    removeEventVisibleWindow(this.handleVisibilityChange);\r\n    addEventVisibleWindow(this.handleVisibilityChange);\r\n\r\n    let observerOptions = {\r\n      threshold: thresholdArray(10)\r\n    };\r\n\r\n    this.adObserver = new IntersectionObserver(this._handleScrollObserver, observerOptions);\r\n    const numBannerShow = zoneConfig.slot < banners.length ? zoneConfig.slot : banners.length;\r\n    this.numBannerShow = numBannerShow;\r\n    const bannerShow = banners.slice(0, numBannerShow);\r\n    const layout = zoneConfig.layout;\r\n\r\n    if (layout) {\r\n      const widthZone = zoneConfig.width;\r\n      const innerWidth = widthZone || window.innerWidth;\r\n      const width = widthZone || innerWidth;\r\n      this.resizeRoot(width, 1);\r\n      if (this.checkZonePopup(zoneConfig)) {\r\n        this.fullScreenAds({ zone: zoneConfig, banners });\r\n        const toWrap = document.getElementById(\'root\');\r\n        if (toWrap) {\r\n          const wrapper = createElementFromHTML(`\r\n          <div id="card">\r\n            <div class="toprow">\r\n              <div id="dismiss-button" aria-label="Đóng quảng cáo" role="button" tabIndex="0">\r\n                <div class="btn skip" style="cursor: pointer;">\r\n                  <svg viewBox="0 0 48 48" fill="#5F6368">\r\n                    <path\r\n                      d="M38 12.83L35.17 10 24 21.17 12.83 10 10 12.83 21.17 24 10 35.17 12.83 38 24 26.83 35.17 38 38 35.17 26.83 24z"></path>\r\n                    <path d="M0 0h48v48H0z" fill="none"></path>\r\n                  </svg>\r\n                </div>\r\n              </div>\r\n            </div>\r\n          </div>\r\n        `);\r\n          wrap(toWrap, wrapper);\r\n          addListener(\'#dismiss-button\', \'click\', () => {\r\n            this.closeAds({ zone: zoneConfig, banners });\r\n          });\r\n          addListener(\'#ad-wrapper\', \'click\', (e) => {\r\n            const targetEl = e.target;\r\n            const cardEl = document.getElementById(\'card\');\r\n            if (cardEl && !cardEl.contains(targetEl)) {\r\n              this.closeAds({ zone: zoneConfig, banners });\r\n            }\r\n          });\r\n        }\r\n      } else if (this.checkZoneSticky(zoneConfig)) {\r\n        this.stickyAds({ zone: zoneConfig, banners });\r\n      }\r\n    // } else if (zoneConfig.width !== undefined && zoneConfig.height !== undefined) {\r\n    //   const width = zoneConfig.width;\r\n    //   const height = zoneConfig.height * numBannerShow;\r\n    //\r\n    //   this.resizeRoot(width, height)\r\n    } else {\r\n      // const innerWidth = window.innerWidth;\r\n      // const aspect_ratio = \'1.91:1\';\r\n      // let [aspectRatio] = aspect_ratio.split(":");\r\n      // aspectRatio = Number.parseFloat(aspectRatio);\r\n      // const heightNoTitle = innerWidth / aspectRatio;\r\n      //\r\n      // const height = numBannerShow * heightNoTitle;\r\n      // this.resizeRoot(innerWidth, height)\r\n    }\r\n\r\n    bannerShow.map(banner => {\r\n      this.pushBanner(banner);\r\n    })\r\n\r\n    setTimeout(() => {\r\n      const elements = document.getElementsByClassName("prevent-default");\r\n      Array.from(elements).forEach(function(element) {\r\n        element.addEventListener(\'click\', managementDisplayAds.preventDefault);\r\n      });\r\n    }, 300)\r\n  },\r\n  checkZoneSticky: function (zone) {\r\n    return zone && zone.display_type === _va_constant.DISPLAY_TYPE.STICKY;\r\n  },\r\n  checkZonePopup: function (zone) {\r\n    return zone && zone.display_type === _va_constant.DISPLAY_TYPE.POPUP;\r\n  },\r\n  handleBindThis: function() {\r\n    Object.keys(this).forEach(property => {\r\n      if (_va_typeof.isFunction(this[property]) && this[property].bind) {\r\n        this[property] = this[property].bind(this);\r\n      }\r\n    })\r\n  },\r\n  fullScreenAds({ zone, banners }) {\r\n    postDataToParent(_va_constant.POST_MESSAGE.EVENT_FULL_SCREEN_ADS, {\r\n      zone,\r\n      banners\r\n    });\r\n    document.body.style.cssText = `\r\n      background-color: rgba(0, 0, 0, 0.4);\r\n      height: 100%;\r\n    `;\r\n  },\r\n  stickyAds({ zone, banners }) {\r\n    postDataToParent(_va_constant.POST_MESSAGE.EVENT_STICKY_ADS, {\r\n      zone,\r\n      banners\r\n    });\r\n  },\r\n  closeAds({ zone, banners }) {\r\n    postDataToParent(_va_constant.POST_MESSAGE.EVENT_CLOSE_ADS, {\r\n      zone,\r\n      banners\r\n    });\r\n  },\r\n  resizeRoot(widthProp, height) {\r\n    let width = widthProp;\r\n    if (widthProp > window.innerWidth) {\r\n      width = window.innerWidth;\r\n    }\r\n    if (!this.checkZonePopup(this.zoneConfig)) {\r\n      postDataToParent(_va_constant.POST_MESSAGE.EVENT_RESIZE_ZONE, {\r\n        width: width,\r\n        height: height\r\n      });\r\n    }\r\n    const root = document.getElementById("root");\r\n    if (root) {\r\n      if (this.dataFullWidthResponsive) {\r\n        const scale = getScale(width, height);\r\n\r\n        resizeByScale(root, scale);\r\n      } else {\r\n        root.style.width = `${width}px`;\r\n        root.style.height = `${height}px`;\r\n      }\r\n    }\r\n  },\r\n  removeAdsElement(bannerId) {\r\n    const idElementBanner = this.getBannerIdElementByBannerId(bannerId)\r\n    const elementBanner = document.getElementById(idElementBanner);\r\n\r\n    if (elementBanner) {\r\n      const zoneConfig = this.zoneConfig;\r\n      this.numBannerShow -= 1;\r\n\r\n      const width = zoneConfig.width;\r\n      const height = zoneConfig.height * this.numBannerShow;\r\n      // this.resizeRoot(width, height);\r\n      this.adObserver.unobserve(elementBanner);\r\n      setTimeout(() => {\r\n        elementBanner.remove();\r\n      }, 100)\r\n    }\r\n  },\r\n  getBannerIdElementByBannerId: function (bannerId) {\r\n    return `item-${bannerId}`;\r\n  },\r\n  pushBanner: function (banner) {\r\n    if (!banner) {\r\n      console.error(\'banner is require\');\r\n      return;\r\n    }\r\n\r\n    if (!this.idDisplayAds) {\r\n      console.error(\'you not init Management display ads\');\r\n      return;\r\n    }\r\n\r\n    const elementDisplayAds = document.getElementById(this.idDisplayAds);\r\n\r\n    if (!elementDisplayAds) {\r\n      console.error(\'not found element show display ads\');\r\n      return;\r\n    }\r\n    this.renderBanner(banner);\r\n    setTimeout(() => {\r\n      this.addEventListenerViewBanner(banner);\r\n      postDataToParent(_va_constant.POST_MESSAGE.EVENT_PUSH_ADS, banner);\r\n    }, 1000)\r\n  },\r\n  eventClickBanner: function (element) {\r\n    if (element) {\r\n      const bannerId = element.getAttribute(_va_constant.DISPLAY_ADS.BANNER_ID);\r\n      const banner = this.getBannerById(bannerId)\r\n      if (banner) {\r\n        postDataToParent(_va_constant.POST_MESSAGE.EVENT_CLICK_ADS, {\r\n          [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n          [_va_constant.DISPLAY_ADS.BANNER_ID]: banner.id,\r\n          banner\r\n        });\r\n      }\r\n    }\r\n  },\r\n  addEventListenerViewBanner: function (banner) {\r\n    const {\r\n      id,\r\n    } = banner;\r\n    const bannerElement = document.getElementById(this.getBannerIdElementByBannerId(id));\r\n    if (bannerElement) {\r\n      this.adObserver.observe(bannerElement);\r\n    }\r\n  },\r\n  renderBanner: function (banner) {\r\n\r\n    if (banner) {\r\n      const {\r\n        id,\r\n        url: bannerUrl,\r\n      } = banner;\r\n\r\n      this.renderContentBannerV1(banner);\r\n    }\r\n  },\r\n  renderButtonVolume(status, dataTarget) {\r\n    return `\r\n      <button\r\n        class="button-volume stop-propagation prevent-default"\r\n        ${_va_constant.VOLUME_BUTTON_ATTRS.DATA_TARGET}="${dataTarget}"\r\n        ${_va_constant.VOLUME_BUTTON_ATTRS.DATA_STATUS}="${status}"\r\n        onclick="managementDisplayAds.clickButtonVolume(this)"\r\n      >\r\n        ${status === _va_constant.VOLUME_STATUS.LISTING ? _va_icons.VOLUME_LISTING : _va_icons.VOLUME_MUTE}\r\n      </button>\r\n    `;\r\n  },\r\n  preventDefault(event) {\r\n    event.preventDefault();\r\n    event.stopPropagation();\r\n  },\r\n  clickButtonVolume(target) {\r\n    if (target) {\r\n      const dataTarget = target.getAttribute(_va_constant.VOLUME_BUTTON_ATTRS.DATA_TARGET);\r\n      const dataStatus = target.getAttribute(_va_constant.VOLUME_BUTTON_ATTRS.DATA_STATUS);\r\n      const elControl = document.querySelector(dataTarget);\r\n      if (elControl) {\r\n        if (dataStatus === _va_constant.VOLUME_STATUS.MUTE) {\r\n          target.setAttribute(_va_constant.VOLUME_BUTTON_ATTRS.DATA_STATUS, _va_constant.VOLUME_STATUS.LISTING);\r\n          target.innerHTML = _va_icons.VOLUME_LISTING;\r\n          elControl.muted = false;\r\n          if (elControl.paused) {\r\n            elControl.play();\r\n          }\r\n        } else {\r\n          target.setAttribute(_va_constant.VOLUME_BUTTON_ATTRS.DATA_STATUS, _va_constant.VOLUME_STATUS.MUTE);\r\n          target.innerHTML = _va_icons.VOLUME_MUTE;\r\n          elControl.muted = true;\r\n        }\r\n      }\r\n    }\r\n  },\r\n  renderLayoutNoTitle: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      id,\r\n      url: bannerUrl\r\n    } = banner;\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n    const {\r\n      style_component = {},\r\n      style_image = {}\r\n    } = layout || {};\r\n    return `<a id="${elementItemId}" href="${bannerUrl}" target="_blank" ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}" class="ads-item layout-no-title" style="${convertObjectToStyle(style_component)}">\r\n    ${this.renderLayoutMediaWrapper(banner)}\r\n  </a>`\r\n  },\r\n  renderLayoutImgOnly: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      id,\r\n      url: bannerUrl\r\n    } = banner;\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n\r\n    const zoneConfig = this.zoneConfig || {};\r\n    const widthZone = zoneConfig.width;\r\n    const heightZone = zoneConfig.height;\r\n\r\n    let {\r\n      style_component = {},\r\n      style_image = {}\r\n    } = layout || {};\r\n    if (!this.dataFullWidthResponsive) {\r\n      layout.style_media_content = {};\r\n      if (widthZone) {\r\n        layout.style_media_content[\'max-width\'] = widthZone;\r\n      }\r\n\r\n      if (heightZone) {\r\n        layout.style_media_content[\'max-height\'] = heightZone;\r\n      }\r\n    }\r\n\r\n    return `<a id="${elementItemId}" href="${bannerUrl}" target="_blank" ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}" class="ads-item layout-img-only" style="${convertObjectToStyle(style_component)}">\r\n    ${this.renderLayoutMediaWrapper(banner)}\r\n  </a>`\r\n  },\r\n  renderLayoutImgAbove: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      id,\r\n      title,\r\n      description,\r\n      url: bannerUrl\r\n    } = banner;\r\n    const {\r\n      style_component = {},\r\n      style_title = {},\r\n      style_description = {},\r\n      style_image = {}\r\n    } = layout || {};\r\n\r\n    if (!title) {\r\n      return this.renderLayoutNoTitle(banner);\r\n    }\r\n\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n    return `<a id="${elementItemId}" href="${bannerUrl}" target="_blank" ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}" class="ads-item layout-img-above" style="${convertObjectToStyle(style_component)}">\r\n    ${this.renderLayoutMediaWrapper(banner)}\r\n    <div class="content-wrapper">\r\n      <div class="layout-title" style="${convertObjectToStyle(style_title)}">\r\n        ${title || \'\'}\r\n      </div>\r\n      <div class="layout-description" style="${convertObjectToStyle(style_description)}">${description || \'\'}</div>\r\n    </div>\r\n  </a>`\r\n  },\r\n  renderLayoutImgSide: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      id,\r\n      title,\r\n      url: bannerUrl,\r\n      description\r\n    } = banner;\r\n    const {\r\n      style_component = {},\r\n      style_title = {},\r\n      style_description = {},\r\n      style_image = {}\r\n    } = layout || {};\r\n\r\n    if (!title) {\r\n      return this.renderLayoutNoTitle(banner);\r\n    }\r\n\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n    return `<a id="${elementItemId}" href="${bannerUrl}" target="_blank" ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}" class="ads-item layout-img-side" style="${convertObjectToStyle(style_component)}">\r\n    ${this.renderLayoutMediaWrapper(banner)}\r\n    <div class="layout-title" style="${convertObjectToStyle(style_title)}">\r\n      ${title || \'\'}\r\n    </div>\r\n    <div class="layout-description" style="${convertObjectToStyle(style_description)}">${description || \'\'}</div>\r\n  </a>`\r\n  },\r\n  renderLayoutTitleTop: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      id,\r\n      title,\r\n      description,\r\n      url: bannerUrl\r\n    } = banner;\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n    const {\r\n      style_component = {},\r\n      style_title = {},\r\n      style_description = {},\r\n      style_image = {}\r\n    } = layout || {};\r\n\r\n    if (!title) {\r\n      return this.renderLayoutNoTitle(banner);\r\n    }\r\n\r\n    return `<a id="${elementItemId}" href="${bannerUrl}" target="_blank" ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}" class="ads-item layout-title-top" style="${convertObjectToStyle(style_component)}">\r\n    <div class="layout-title" style="${convertObjectToStyle(style_title)}">\r\n      ${title || \'\'}\r\n    </div>\r\n    ${this.renderLayoutMediaWrapper(banner)}\r\n    <div class="layout-description" style="${convertObjectToStyle(style_description)}">${description || \'\'}</div>\r\n  </a>`\r\n  },\r\n  renderLayoutMediaWrapper: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      media\r\n    } = banner;\r\n    const {\r\n      style_image = {}\r\n    } = layout || {};\r\n\r\n    if (_va_typeof.isEmpty(media)) {\r\n      return \'\';\r\n    }\r\n\r\n    return `\r\n    <div class="layout-media" style="${convertObjectToStyle(style_image)}">\r\n      ${this.renderLayoutMedia(banner)}\r\n    </div>\r\n    `;\r\n  },\r\n  getMediaShow: function (banner) {\r\n    let {\r\n      media\r\n    } = banner;\r\n\r\n    const landscape = media.find(\r\n      data => data.type === "landscape"\r\n    );\r\n    const square = media.find(data => data.type === "square");\r\n\r\n    const aspectRatio = this.getAspectRatio();\r\n\r\n    let mediaShow;\r\n    if (aspectRatio === 1.91) {\r\n      mediaShow = landscape || media[0];\r\n    } else {\r\n      mediaShow = square || media[0];\r\n    }\r\n\r\n    return mediaShow;\r\n  },\r\n  getAspectRatio: function () {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    const {\r\n      layout_type,\r\n      style_image = {}\r\n    } = layout || {};\r\n\r\n    let aspectRatioDefault = \'1.91:1\';\r\n    if (layout_type === _va_constant.ZONE_LAYOUT.MEDIA_ABOVE || layout_type === _va_constant.ZONE_LAYOUT.MEDIA_ONLY) {\r\n      aspectRatioDefault = \'1:1\';\r\n    }\r\n    const aspect_ratio = (style_image || {}).aspect_ratio || aspectRatioDefault;\r\n\r\n    let [aspectRatio] = aspect_ratio.split(":");\r\n    aspectRatio = Number.parseFloat(aspectRatio);\r\n\r\n    return aspectRatio;\r\n  },\r\n  renderLayoutMedia: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      id,\r\n      title,\r\n      description,\r\n      media\r\n    } = banner;\r\n    const {\r\n      layout_type,\r\n      style_component = {},\r\n      style_title = {},\r\n      style_description = {},\r\n      style_image = {},\r\n      style_media_content = {}\r\n    } = layout || {};\r\n\r\n    if (_va_typeof.isEmpty(media)) {\r\n      return \'\';\r\n    }\r\n\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n    let dataTarget = elementItemId;\r\n\r\n    const mediaShow = this.getMediaShow(banner);\r\n\r\n    const aspectRatio = this.getAspectRatio();\r\n\r\n    if (this.checkBannerUseImage(banner)) {\r\n      return `<div\r\n        style="padding-top: ${100 / aspectRatio}%"\r\n        class="media-container"\r\n      >\r\n        <img\r\n          alt="${title || \'\'}"\r\n          class="media"\r\n          width="100%"\r\n          src="${mediaShow.data}"\r\n          style="${convertObjectToStyle(style_media_content)}"\r\n        />\r\n      </div>`\r\n    } else if (this.checkBannerUseVideo(banner)) {\r\n      dataTarget = `#${elementItemId} .media-source`\r\n      setTimeout(() => {\r\n        this.addControlVideo(banner)\r\n        document.querySelector(`#${elementItemId} .button-skip`).addEventListener(\'click\', function (event) {\r\n          managementDisplayAds.skipAds(event, id);\r\n        })\r\n      }, 300);\r\n      return `<div\r\n        style="padding-top: ${100 / aspectRatio}%"\r\n        class="media-container"\r\n      >\r\n        <video\r\n          ${_va_constant.DISPLAY_ADS.BANNER_ID}="${banner.id}"\r\n          title="${title || \'\'}"\r\n          style="${convertObjectToStyle(style_media_content)}"\r\n          class="media media-source"\r\n          autoplay\r\n          width="100%"\r\n          muted\r\n          playsinline\r\n        >\r\n          <source src="${mediaShow.data}" />\r\n          Your browser does not support the video tag.\r\n        </video>\r\n        ${this.renderButtonVolume(_va_constant.VOLUME_STATUS.MUTE, dataTarget)}\r\n        <button\r\n          class="button-skip"\r\n        >\r\n          Bỏ qua\r\n        </button>\r\n      </div>`\r\n    } else if (this.checkBannerUseAudio(banner)) {\r\n      dataTarget = `#${elementItemId} .media-source`;\r\n\r\n      setTimeout(() => {\r\n        this.addControlVideo(banner);\r\n      }, 300);\r\n      return `<div\r\n        style="padding-top: ${100 / aspectRatio}%"\r\n        class="media-container position-relative"\r\n      >\r\n        <img\r\n          src="${mediaShow.thumbnail}"\r\n          width="100%"\r\n          class="media"\r\n          alt="preview-wrapper"\r\n          style="${convertObjectToStyle(style_media_content)}"\r\n        />\r\n        <audio\r\n          controls\r\n          autoplay\r\n          muted\r\n          title="${title || \'\'}"\r\n          class="media-source"\r\n          ${_va_constant.DISPLAY_ADS.BANNER_ID}="${banner.id}"\r\n        >\r\n          <source src="${mediaShow.data}" />\r\n          <p>Your browser doesn\'t support HTML5 video</p>\r\n        </audio>\r\n        ${this.renderButtonVolume(_va_constant.VOLUME_STATUS.MUTE, dataTarget)}\r\n      </div>`\r\n    }\r\n\r\n    return \'\';\r\n  },\r\n  renderLayoutTextOnly: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    let {\r\n      id,\r\n      title,\r\n      description,\r\n      url: bannerUrl\r\n    } = banner;\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n    const {\r\n      style_component = {},\r\n      style_title = {},\r\n      style_description = {}\r\n    } = layout || {};\r\n    return `<a id=\'${elementItemId}\' href="${bannerUrl}" target="_blank" ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}" class="ads-item layout-text-only" style="${convertObjectToStyle(style_component)}">\r\n    <div class="content-wrapper">\r\n      <div class="layout-title" style="${convertObjectToStyle(style_title)}">\r\n        ${title || \'\'}\r\n      </div>\r\n      <div class="layout-description" style="${convertObjectToStyle(style_description)}">${description || \'\'}</div>\r\n    </div>\r\n  </a>`\r\n  },\r\n  renderContentBannerV2: function (banner) {\r\n    const {\r\n      layout\r\n    } = this.zoneConfig || {};\r\n\r\n    const {\r\n      layout_type: type\r\n    } = layout || {};\r\n    switch (type) {\r\n      case _va_constant.ZONE_LAYOUT.MEDIA_ABOVE:\r\n        return this.renderLayoutImgAbove(banner);\r\n      case _va_constant.ZONE_LAYOUT.MEDIA_SIDE:\r\n        return this.renderLayoutImgSide(banner);\r\n      case _va_constant.ZONE_LAYOUT.TITLE_TOP:\r\n        return this.renderLayoutTitleTop(banner);\r\n      case _va_constant.ZONE_LAYOUT.MEDIA_ONLY:\r\n        return this.renderLayoutImgOnly(banner);\r\n      case _va_constant.ZONE_LAYOUT.TEXT_ONLY:\r\n      default:\r\n        return this.renderLayoutTextOnly(banner);\r\n    }\r\n  },\r\n  renderContentBannerV1: function (banner) {\r\n    let {\r\n      id,\r\n      media_type,\r\n      media = [],\r\n      url: bannerUrl,\r\n      threshold,\r\n    } = banner;\r\n\r\n    const {\r\n      width = 300,\r\n      height = 300,\r\n      layout\r\n    } = this.zoneConfig || {};\r\n    const widthPx = `${width}px`;\r\n    const heightPx = `${height}px`;\r\n    let content = \'\';\r\n    const elementDisplayAds = document.getElementById(this.idDisplayAds);\r\n    const elementItemId = this.getBannerIdElementByBannerId(id);\r\n    if (layout) {\r\n      elementDisplayAds.insertAdjacentHTML(\'beforeend\', this.renderContentBannerV2(banner));\r\n\r\n      setTimeout(() => {\r\n        const that = this;\r\n        document.querySelector(`.ads-item`).addEventListener(\'click\', function (event) {\r\n          that.eventClickBanner(this);\r\n        })\r\n      }, 100);\r\n      return;\r\n    } else {\r\n      // elementDisplayAds.insertAdjacentHTML(\'beforeend\', this.renderLayoutNoTitle(banner));\r\n      return;\r\n    }\r\n\r\n    if (media.length >= 1) {\r\n      const {\r\n        data,\r\n        thumbnail,\r\n        type,\r\n      } = media[0] || {};\r\n\r\n      const styleContentFix = this.dataFullWidthResponsive ? "" : `width: ${widthPx}; height: ${heightPx};`;\r\n      const classContentResponsive = this.dataFullWidthResponsive ? "responsive-child" : "";\r\n      let dataTarget = elementItemId;\r\n      switch (media_type) {\r\n        case _va_constant.MEDIA_TYPES.VIDEO:\r\n          content =  `\r\n            <div\r\n              id=\'${elementItemId}\'\r\n              ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}"\r\n              class="item ${classContentResponsive}"\r\n              style="${styleContentFix}"\r\n            >\r\n              <video\r\n                ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}"\r\n                class="media-video ${classContentResponsive} media-source"\r\n                style="${styleContentFix} display: block"\r\n                width="${widthPx}"\r\n                height="${heightPx}"\r\n                title="${data}"\r\n                onclick=\'managementDisplayAds.clickVideo(this, "${bannerUrl}")\'\r\n                autoplay\r\n                muted\r\n                playsinline\r\n                loop\r\n              >\r\n                <source src="${data}" />\r\n              </video>\r\n            </div>`;\r\n          dataTarget = `#${elementItemId} .media-source`\r\n          setTimeout(() => {\r\n            this.addControlVideo(banner)\r\n          }, 300);\r\n        break;\r\n        case _va_constant.MEDIA_TYPES.AUDIO:\r\n          const audioId = `audio-${id}`;\r\n          const audioInterval = setInterval(function() {\r\n            const audioEl = document.getElementById(audioId);\r\n            if (!audioEl) return;\r\n            var playedPromise = audioEl.play();\r\n            // audioEl.click();\r\n            if (playedPromise) {\r\n              playedPromise.then(rs => {\r\n                clearInterval(audioInterval);\r\n              }).catch((e) => {\r\n                console.log(e)\r\n                if (e.name === \'NotAllowedError\' || e.name === \'NotSupportedError\') {\r\n                  console.log(e.name);\r\n                }\r\n              }).then(() => {\r\n                console.log("playing sound !!!");\r\n              });\r\n            }\r\n          }, 1000);\r\n          content = `\r\n            <a\r\n              id="${elementItemId}"\r\n              ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}"\r\n              href="${bannerUrl}"\r\n              target="_blank"\r\n              data-total-view-time="0"\r\n              data-last-view-started="0"\r\n              class="item ${classContentResponsive}"\r\n              style="${styleContentFix}"\r\n            >\r\n              <img class="banner-audio ${classContentResponsive}" alt="${bannerUrl}" src="${thumbnail}" style="${styleContentFix} object-fit: fill; display: block">\r\n              <audio class="banner-audio-data" id="${audioId}" autoplay muted loop autoplay muted playsinline loop>\r\n                <source src="${data}">\r\n                <p>Your browser doesn\'t support HTML5 video</p>\r\n              </audio>\r\n            </a>\r\n            `;\r\n          dataTarget = `#${elementItemId} audio`\r\n        break;\r\n        case _va_constant.MEDIA_TYPES.TEXT:\r\n          content = `<a\r\n            id="${elementItemId}"\r\n            ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}"\r\n            href="${bannerUrl}"\r\n            target="_blank"\r\n            data-total-view-time="0"\r\n            data-last-view-started="0"\r\n            class="item ${classContentResponsive}"\r\n            style="${styleContentFix}"\r\n          >\r\n            <div class="banner-text ql-container ql-snow" style="${styleContentFix} display: block" class="${classContentResponsive}">\r\n              <div class="ql-editor">${data}</div>\r\n            </div>\r\n          </a>`;\r\n        break;\r\n        case _va_constant.MEDIA_TYPES.IMAGE:\r\n        default:\r\n          content = `<a\r\n            id="${elementItemId}"\r\n            ${_va_constant.DISPLAY_ADS.BANNER_ID}="${id}"\r\n            href="${bannerUrl}"\r\n            target="_blank"\r\n            data-total-view-time="0"\r\n            data-last-view-started="0"\r\n            class="item ${classContentResponsive}"\r\n            style="${styleContentFix}"\r\n          >\r\n            <img alt="${bannerUrl}" src="${data}" style="${styleContentFix} object-fit: fill; display: block" class="${classContentResponsive}">\r\n          </a>`;\r\n      }\r\n\r\n      const contentButtonSkip = `\r\n      <button\r\n        class="button-skip"\r\n      >\r\n        Bỏ qua\r\n      </button>\r\n    `;\r\n\r\n      const contentButtonAudio = this.renderButtonVolume(_va_constant.VOLUME_STATUS.MUTE, dataTarget);\r\n\r\n      const scale = getScale(width, height);\r\n      const scaleStyle = `padding-top: ${scale}%`\r\n\r\n      content = `\r\n        <div\r\n          class="wrapper ${this.dataFullWidthResponsive ? "responsive" : ""}"\r\n          style="${this.dataFullWidthResponsive ? scaleStyle : ""}"\r\n        >\r\n          ${content}\r\n          ${_va_constant.MEDIA_THRESHOLD.includes(media_type) ? contentButtonSkip : ""}\r\n          ${_va_constant.MEDIA_AUDIO.includes(media_type) ? contentButtonAudio : ""}\r\n        <div>\r\n      `;\r\n\r\n      elementDisplayAds.insertAdjacentHTML(\'beforeend\', content);\r\n      setTimeout(() => {\r\n        document.querySelector(`#${elementItemId} .button-skip`).addEventListener(\'click\', function (event) {\r\n          managementDisplayAds.skipAds(event, id);\r\n        })\r\n      }, 100);\r\n    }\r\n  },\r\n  _handleScrollObserver: function(entries) {\r\n    /*\r\n      Looping through all entries, which are observed.\r\n    */\r\n    entries.forEach((entry) => {\r\n      /*\r\n        With managementDisplayAds condition, we check whether the element is in the current viewport,\r\n        respectively if the entry intersects with the viewport.\r\n        If true, then we add the enter class and call the unobserve function.\r\n        Because we don’t have to observe it anymore.\r\n      */\r\n      let adBox = entry.target;\r\n      if (adBox) {\r\n        if(entry.isIntersecting) {\r\n          this.handleIntersecting(entry);\r\n        } else {\r\n          this.handleNotIntersecting(entry);\r\n        }\r\n      }\r\n    });\r\n  },\r\n  handleNotIntersecting(entry) {\r\n    const adBox = entry.target;\r\n    const bannerId = adBox.getAttribute(_va_constant.DISPLAY_ADS.BANNER_ID);\r\n    const banner = this.getBannerById(bannerId);\r\n    if (adBox) {\r\n      this.handleAdsNotViewing(adBox, banner);\r\n    }\r\n  },\r\n  checkBannerUseImage (banner) {\r\n    const mediaShow = this.getMediaShow(banner);\r\n    if (mediaShow) {\r\n      return _va_constant.REGEX_MEDIA.URL_IMAGE_REGEX.test(mediaShow.data);\r\n    }\r\n  },\r\n  checkBannerUseAudio (banner) {\r\n    const mediaShow = this.getMediaShow(banner);\r\n    if (mediaShow) {\r\n      return _va_constant.REGEX_MEDIA.URL_AUDIO_REGEX.test(mediaShow.data);\r\n    }\r\n  },\r\n  checkBannerUseVideo (banner) {\r\n    const mediaShow = this.getMediaShow(banner);\r\n    if (mediaShow) {\r\n      return _va_constant.REGEX_MEDIA.URL_VIDEO_REGEX.test(mediaShow.data);\r\n    }\r\n  },\r\n  checkBannerUseMedia (banner) {\r\n    return this.checkBannerUseVideo(banner) || this.checkBannerUseAudio(banner);\r\n  },\r\n  isBannerUseThreshold (banner) {\r\n    return this.checkBannerUseMedia (banner) && !(banner.threshold === undefined || banner.threshold === null);\r\n  },\r\n  handleAdsViewing (adBox, banner) {\r\n    if (adBox && banner) {\r\n      const bannerId = banner.id;\r\n      const viewportStatus = adBox.getAttribute(_va_constant.DISPLAY_ADS.VIEWPORT_STATUS);\r\n      const adsStatus = adBox.getAttribute(_va_constant.DISPLAY_ADS.ADS_VIEW_STATUS);\r\n      const adsTriggerTimeStatus = adBox.getAttribute(_va_constant.DISPLAY_ADS.ADS_TRIGGER_TIME_STATUS);\r\n      const mediaThresholdStatus = adBox.getAttribute(_va_constant.DISPLAY_ADS.MEDIA_THRESHOLD_STATUS);\r\n      if (viewportStatus !== _va_constant.ADS_VIEWPORT_STATUS.VIEWING) {\r\n        adBox.setAttribute(_va_constant.DISPLAY_ADS.VIEWPORT_STATUS, _va_constant.ADS_VIEWPORT_STATUS.VIEWING);\r\n      }\r\n\r\n      if (this.isBannerUseThreshold(banner)) {\r\n        this.startVideo(bannerId);\r\n      }\r\n\r\n      if (\r\n        adsStatus !== _va_constant.ADS_VIEW_STATUS.VIEWED &&\r\n        !this.timeoutIntersectionObserver[bannerId] &&\r\n        !this.isBannerUseThreshold(banner)\r\n      ) {\r\n        this.visibleAds[bannerId] = adBox;\r\n        this.timeoutIntersectionObserver[bannerId] = setTimeout(() => {\r\n          this.handleViewedAds(adBox, banner)\r\n        }, _va_constant.ADS_CONFIG.TIME_ACCEPT_VIEWPORT);\r\n      }\r\n\r\n      if (\r\n        banner.data_trigger_time &&\r\n        adsTriggerTimeStatus !== _va_constant.ADS_TRIGGER_TIME_STATUS.SUCCESS &&\r\n        !this.timeoutTriggerTime[bannerId]\r\n      ) {\r\n        this.timeoutTriggerTime[bannerId] = setTimeout(() => {\r\n          this.handleTriggerTimeAds(adBox, banner)\r\n        }, banner.data_trigger_time * 1000);\r\n      }\r\n    }\r\n  },\r\n  handleAdsNotViewing (adBox, banner) {\r\n    if (adBox && banner) {\r\n      const bannerId = banner.id;\r\n      const viewportStatus = adBox.getAttribute(_va_constant.DISPLAY_ADS.VIEWPORT_STATUS);\r\n      const adsStatus = adBox.getAttribute(_va_constant.DISPLAY_ADS.ADS_VIEW_STATUS);\r\n      const mediaThresholdStatus = adBox.getAttribute(_va_constant.DISPLAY_ADS.MEDIA_THRESHOLD_STATUS);\r\n\r\n      if (this.visibleAds[bannerId]) {\r\n        delete this.visibleAds[bannerId];\r\n      }\r\n\r\n      clearTimeout(this.timeoutIntersectionObserver[bannerId]);\r\n      delete this.timeoutIntersectionObserver[bannerId];\r\n\r\n      clearTimeout(this.timeoutTriggerTime[bannerId]);\r\n      delete this.timeoutTriggerTime[bannerId];\r\n\r\n      if (viewportStatus !== _va_constant.ADS_VIEWPORT_STATUS.NOT_VIEW) {\r\n        adBox.setAttribute(_va_constant.DISPLAY_ADS.VIEWPORT_STATUS, _va_constant.ADS_VIEWPORT_STATUS.NOT_VIEW);\r\n      }\r\n\r\n      if (this.isBannerUseThreshold(banner)) {\r\n        this.pauseVideo(bannerId);\r\n      }\r\n    }\r\n  },\r\n  handleIntersecting(entry) {\r\n    let adBox = entry && entry.target;\r\n    if (adBox) {\r\n      const bannerId = adBox.getAttribute(_va_constant.DISPLAY_ADS.BANNER_ID);\r\n      const banner = this.getBannerById(bannerId);\r\n\r\n      if (entry.intersectionRatio >= _va_constant.ADS_CONFIG.THRESHOLD_VIEWPORT) {\r\n        this.handleAdsViewing(adBox, banner);\r\n      } else {\r\n        this.handleAdsNotViewing(adBox, banner);\r\n      }\r\n    }\r\n  },\r\n  handleUnobserve: function (adBox, banner) {\r\n    let isCanUnobserve = true;\r\n\r\n    if (banner.data_trigger_time && adBox.getAttribute(_va_constant.DISPLAY_ADS.ADS_TRIGGER_TIME_STATUS) !== _va_constant.ADS_TRIGGER_TIME_STATUS.SUCCESS) {\r\n      isCanUnobserve = false;\r\n    }\r\n\r\n    if (adBox.getAttribute(_va_constant.DISPLAY_ADS.ADS_VIEW_STATUS) !== _va_constant.ADS_VIEW_STATUS.VIEWED) {\r\n      isCanUnobserve = false;\r\n    }\r\n\r\n    if (isCanUnobserve) {\r\n      this.adObserver.unobserve(adBox);\r\n    }\r\n  },\r\n  handleTriggerTimeAds: function (adBox, banner) {\r\n    const bannerId = banner && banner.id;\r\n    if (adBox.getAttribute(_va_constant.DISPLAY_ADS.ADS_TRIGGER_TIME_STATUS) !== _va_constant.ADS_TRIGGER_TIME_STATUS.SUCCESS) {\r\n      adBox.setAttribute(_va_constant.DISPLAY_ADS.ADS_TRIGGER_TIME_STATUS, _va_constant.ADS_TRIGGER_TIME_STATUS.SUCCESS);\r\n      const data = {\r\n        [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n        [_va_constant.DISPLAY_ADS.BANNER_ID]: bannerId,\r\n        banner\r\n      };\r\n\r\n      postDataToParent(_va_constant.POST_MESSAGE.EVENT_TRIGGER_TIME_ADS, data);\r\n      clearTimeout(this.timeoutTriggerTime[bannerId])\r\n      delete this.timeoutTriggerTime[bannerId];\r\n      this.handleUnobserve(adBox, banner);\r\n    }\r\n  },\r\n  handleViewedAds: function (adBox, banner) {\r\n    const bannerId = banner && banner.id;\r\n    if (adBox.getAttribute(_va_constant.DISPLAY_ADS.ADS_VIEW_STATUS) !== _va_constant.ADS_VIEW_STATUS.VIEWED) {\r\n      adBox.setAttribute(_va_constant.DISPLAY_ADS.ADS_VIEW_STATUS, _va_constant.ADS_VIEW_STATUS.VIEWED);\r\n      const data = {\r\n        [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n        [_va_constant.DISPLAY_ADS.BANNER_ID]: bannerId,\r\n        banner\r\n      };\r\n\r\n      postDataToParent(_va_constant.POST_MESSAGE.EVENT_VIEW_ADS, data);\r\n      clearTimeout(this.timeoutIntersectionObserver[bannerId])\r\n      delete this.timeoutIntersectionObserver[bannerId];\r\n      this.handleUnobserve(adBox, banner);\r\n    }\r\n  },\r\n  handleVisibilityChange: function(event) {\r\n    if (!getVisibleByEvent(event)) {\r\n      this.isVisibleWindow = false;\r\n    } {\r\n      this.isVisibleWindow = true;\r\n    }\r\n\r\n    for (const [bannerId, adBox] of Object.entries(this.visibleAds)) {\r\n      const banner = this.getBannerById(bannerId);\r\n      if (this.isVisibleWindow) {\r\n        this.handleAdsViewing(adBox, banner);\r\n      } else {\r\n        this.handleAdsNotViewing(adBox, banner);\r\n      }\r\n    }\r\n  },\r\n  resetVideo: function (bannerId) {\r\n    const elementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const banner = this.getBannerById(bannerId);\r\n    const video = document.querySelector(`#${elementId} .media-source`);\r\n    if (banner) {\r\n      this.clearControlVideoById(bannerId);\r\n\r\n      if (video) {\r\n        // video.removeEventListener("play", this.videoStartedPlaying);\r\n\r\n        video.removeEventListener("ended", this.videoEndedPlaying);\r\n        video.removeEventListener("pause", this.videoStoppedPlaying);\r\n        video.removeEventListener(\'timeupdate\', this.videoTimeupdate);\r\n      }\r\n\r\n      this.addControlVideo(banner);\r\n    }\r\n  },\r\n  startVideo: function (bannerId) {\r\n    const elementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const video = document.querySelector(`#${elementId} .media-source`);\r\n    if (video.paused) {\r\n      video.play();\r\n    }\r\n  },\r\n  pauseVideo: function (bannerId) {\r\n    const elementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const video = document.querySelector(`#${elementId} .media-source`);\r\n    if (video) {\r\n      if (video) {\r\n        video.pause();\r\n      }\r\n    }\r\n  },\r\n  removeControlVideo: function(id) {\r\n    const elementId = this.getBannerIdElementByBannerId(id);\r\n    const video = document.querySelector(`#${elementId} .media-source`);\r\n    if (this.controlVideo[id]) {\r\n      clearInterval(this.controlVideo[id].timeInterval);\r\n    }\r\n    if (video) {\r\n      video.removeEventListener("play", this.videoStartedPlaying);\r\n      video.removeEventListener("pause", this.videoStoppedPlaying);\r\n      video.removeEventListener(\'timeupdate\', this.videoTimeupdate);\r\n    }\r\n  },\r\n  clearControlVideoById: function (id) {\r\n    if (this.controlVideo[id]) {\r\n      const control = this.controlVideo[id] || {};\r\n      const {\r\n        timeInterval\r\n      } = control;\r\n      clearInterval(timeInterval);\r\n      delete this.controlVideo[id];\r\n    }\r\n  },\r\n  getBannerIdByEventVideo: function (event) {\r\n    if (event.target && event.target.getAttribute) {\r\n      return event.target.getAttribute(_va_constant.DISPLAY_ADS.BANNER_ID);\r\n    }\r\n  },\r\n  addControlVideo: function (banner) {\r\n    const bannerId = banner.id;\r\n    const elementId = this.getBannerIdElementByBannerId(banner.id);\r\n    const video = document.querySelector(`#${elementId} .media-source`);\r\n    this.clearControlVideoById(bannerId);\r\n\r\n    this.controlVideo[bannerId] = {\r\n      bannerId,\r\n      elementId,\r\n      timeDuration: 0,\r\n      timeInterval: null,\r\n      status: _va_constant.VIDEO_STATUS.VIDEO_PAUSE,\r\n      timeStart: null,\r\n      timePause: null,\r\n      timeEnded: null,\r\n    }\r\n    if (video) {\r\n      this.videoStartedPlaying(banner);\r\n\r\n      video.addEventListener("ended", this.videoEndedPlaying);\r\n      video.addEventListener("pause", this.videoStoppedPlaying);\r\n      video.addEventListener(\'timeupdate\', this.videoTimeupdate);\r\n    }\r\n  },\r\n  videoStartedPlaying: function(banner) {\r\n\r\n    const bannerId = banner.id;\r\n    const bannerElementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const bannerElement = document.getElementById(bannerElementId);\r\n    const video = document.querySelector(`#${bannerElementId} .media-source`);\r\n\r\n    if (this.checkViewingVideoByBanner(banner.id) && !this.controlVideo[bannerId].timeInterval) {\r\n      this.controlVideo[bannerId].timeInterval = setInterval(() => {\r\n        if (!(video)) {\r\n          clearInterval(this.controlVideo[bannerId].timeInterval)\r\n          this.controlVideo[bannerId].timeInterval = null;\r\n          return;\r\n        }\r\n\r\n        if (this.checkViewingVideoByBanner(banner.id)) {\r\n          const duration = Math.floor(video.duration);\r\n          const timeDurationNew = this.controlVideo[bannerId].timeDuration + 1\r\n          this.controlVideo[bannerId].timeDuration = timeDurationNew;\r\n          const banner = this.getBannerById(bannerId);\r\n\r\n          if (banner) {\r\n            postDataToParent(_va_constant.POST_MESSAGE.EVENT_HEART_BIT_ADS, {\r\n              [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n              [_va_constant.DISPLAY_ADS.BANNER_ID]: banner.id,\r\n              timeDuration: timeDurationNew,\r\n              banner\r\n            });\r\n\r\n            if (this.isBannerUseThreshold(banner) && banner.threshold === timeDurationNew) {\r\n              this.showButtonSkip(bannerId);\r\n              postDataToParent(_va_constant.POST_MESSAGE.EVENT_VIEW_ADS, {\r\n                [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n                [_va_constant.DISPLAY_ADS.BANNER_ID]: banner.id,\r\n                timeDuration: timeDurationNew,\r\n                banner\r\n              });\r\n            }\r\n          }\r\n\r\n          if (duration <= timeDurationNew && bannerElement.getAttribute(_va_constant.DISPLAY_ADS.MEDIA_STATUS) !== _va_constant.MEDIA_STATUS.ENDED) {\r\n            bannerElement.setAttribute(_va_constant.DISPLAY_ADS.MEDIA_STATUS, _va_constant.MEDIA_STATUS.ENDED);\r\n            postDataToParent(_va_constant.POST_MESSAGE.EVENT_END_VIDEO_ADS, {\r\n              [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n              [_va_constant.DISPLAY_ADS.BANNER_ID]: banner.id,\r\n              banner\r\n            });\r\n          }\r\n\r\n          if ((this.isBannerUseThreshold(banner) && duration <= timeDurationNew && banner.threshold <= timeDurationNew) || (!this.isBannerUseThreshold(banner) && duration <= timeDurationNew)) {\r\n            clearInterval(this.controlVideo[bannerId].timeInterval)\r\n            this.controlVideo[bannerId].timeInterval = null;\r\n\r\n            clearTimeout(this.timeoutIntersectionObserver[bannerId])\r\n            delete this.timeoutIntersectionObserver[bannerId];\r\n            this.adObserver.unobserve(bannerElement);\r\n\r\n            this.removeControlVideo(bannerId);\r\n          }\r\n        }\r\n      }, 1000);\r\n      bannerElement.setAttribute(_va_constant.DISPLAY_ADS.MEDIA_THRESHOLD_STATUS, _va_constant.VIDEO_STATUS.VIDEO_PLAY);\r\n      this.controlVideo[bannerId].status = _va_constant.VIDEO_STATUS.VIDEO_PLAY;\r\n      this.controlVideo[bannerId].timeStart = Date.now();\r\n    }\r\n  },\r\n  getBannerById: function (id) {\r\n    if (_va_typeof.isArray(this.banners)) {\r\n      return this.banners.find(banner => banner.id == id)\r\n    }\r\n  },\r\n  videoStoppedPlaying: function (event) {\r\n    const bannerId = this.getBannerIdByEventVideo(event);\r\n    const bannerElementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const bannerElement = document.getElementById(bannerElementId);\r\n    if (this.checkConditionControlVideo(event)) {\r\n      bannerElement.setAttribute(_va_constant.DISPLAY_ADS.MEDIA_THRESHOLD_STATUS, _va_constant.VIDEO_STATUS.VIDEO_PAUSE);\r\n      this.controlVideo[bannerId].status = _va_constant.VIDEO_STATUS.VIDEO_PAUSE;\r\n      this.controlVideo[bannerId].timePause = Date.now();\r\n    }\r\n  },\r\n  videoEndedPlaying: function (event) {\r\n    event.target.play();\r\n    const bannerId = this.getBannerIdByEventVideo(event);\r\n    const bannerElementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const bannerElement = document.getElementById(bannerElementId);\r\n\r\n    if (this.checkConditionControlVideo(event)) {\r\n      const banner = this.getBannerById(bannerId);\r\n      if (banner.threshold === -1 || banner.threshold === 0 || banner.threshold === undefined || banner.threshold === null) {\r\n        const duration = Math.floor(event.target.duration);\r\n        const durationCurrent = this.controlVideo[bannerId].timeDuration;\r\n        if (durationCurrent < duration) {\r\n          postDataToParent(_va_constant.POST_MESSAGE.EVENT_HEART_BIT_ADS, {\r\n            [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n            [_va_constant.DISPLAY_ADS.BANNER_ID]: banner.id,\r\n            timeDuration: duration,\r\n            banner\r\n          });\r\n        }\r\n\r\n        postDataToParent(_va_constant.POST_MESSAGE.EVENT_VIEW_ADS, {\r\n          [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n          [_va_constant.DISPLAY_ADS.BANNER_ID]: banner.id,\r\n          banner\r\n        });\r\n      }\r\n\r\n      bannerElement.setAttribute(_va_constant.DISPLAY_ADS.MEDIA_THRESHOLD_STATUS, _va_constant.VIDEO_STATUS.VIDEO_ENDED);\r\n      this.controlVideo[bannerId].status = _va_constant.VIDEO_STATUS.VIDEO_ENDED;\r\n      this.controlVideo[bannerId].timeEnded = Date.now();\r\n    }\r\n  },\r\n  checkConditionControlVideo: function (event) {\r\n    const bannerId = this.getBannerIdByEventVideo(event);\r\n    const bannerElementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const bannerElement = document.getElementById(bannerElementId);\r\n    const mediaThresholdStatus = bannerElement.getAttribute(_va_constant.DISPLAY_ADS.MEDIA_THRESHOLD_STATUS);\r\n\r\n    return (\r\n      bannerId &&\r\n      bannerElement &&\r\n      this.controlVideo[bannerId] &&\r\n      mediaThresholdStatus !== _va_constant.VIDEO_STATUS.VIDEO_ENDED &&\r\n      this.controlVideo[bannerId].status !== _va_constant.VIDEO_STATUS.VIDEO_ENDED &&\r\n      this.checkViewingVideoByBanner(bannerId)\r\n    );\r\n  },\r\n  checkViewingVideoByBanner: function (bannerId) {\r\n    const elementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const elAds = document.getElementById(elementId);\r\n    if (elAds) {\r\n      const viewPostStatus = elAds.getAttribute(_va_constant.DISPLAY_ADS.VIEWPORT_STATUS);\r\n      return viewPostStatus === _va_constant.ADS_VIEWPORT_STATUS.VIEWING;\r\n    }\r\n  },\r\n  videoTimeupdate: function (event) {\r\n    const bannerId = this.getBannerIdByEventVideo(event);\r\n    const banner = this.getBannerById(bannerId);\r\n    if (\r\n      bannerId &&\r\n      this.checkViewingVideoByBanner(bannerId) &&\r\n      this.controlVideo[bannerId] &&\r\n      this.controlVideo[bannerId].status !== _va_constant.VIDEO_STATUS.VIDEO_PLAY &&\r\n      this.controlVideo[bannerId].timeStart === null\r\n    ) {\r\n      this.videoStartedPlaying(banner)\r\n    }\r\n  },\r\n  clickVideo(element, url) {\r\n    if (element.tagName === \'VIDEO\') {\r\n      window.open(url, "_blank");\r\n    }\r\n  },\r\n  showButtonSkip(bannerId) {\r\n    const elementId = this.getBannerIdElementByBannerId(bannerId);\r\n    const buttonSkipElement = document.querySelector(`#${elementId} .button-skip`);\r\n    const banner = this.getBannerById(bannerId);\r\n\r\n    if (buttonSkipElement) {\r\n      postDataToParent(_va_constant.POST_MESSAGE.EVENT_SHOW_BUTTON_SKIP_ADS, {\r\n        [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n        [_va_constant.DISPLAY_ADS.BANNER_ID]: bannerId,\r\n        banner\r\n      });\r\n\r\n      buttonSkipElement.style.display = "block";\r\n    }\r\n  },\r\n  skipAds(event, bannerId) {\r\n    event.preventDefault();\r\n    event.stopPropagation();\r\n    if (\r\n      bannerId &&\r\n      this.controlVideo[bannerId]\r\n    ) {\r\n      const banner = this.getBannerById(bannerId);\r\n      const durationCurrent = this.controlVideo[bannerId].timeDuration;\r\n\r\n      if (banner) {\r\n        postDataToParent(_va_constant.POST_MESSAGE.EVENT_SKIP_ADS, {\r\n          [_va_constant.DISPLAY_ADS.ZONE_ID]: this.zoneId,\r\n          [_va_constant.DISPLAY_ADS.BANNER_ID]: banner.id,\r\n          timeDuration: durationCurrent,\r\n          banner\r\n        });\r\n        this.removeControlVideo(bannerId);\r\n      }\r\n\r\n      this.removeAdsElement(bannerId);\r\n    }\r\n  },\r\n}\r\n\r\nmodule.exports = managementDisplayAds;\r\n\n\n//# sourceURL=webpack:///./assets/js/management-display-ads.js?')},"./assets/js/show-ads.js":(__unused_webpack_module,__unused_webpack_exports,__webpack_require__)=>{eval('const _va_constant = __webpack_require__(/*! ./constant */ "./assets/js/constant.js");\r\nconst managementDisplayAds = __webpack_require__(/*! ./management-display-ads */ "./assets/js/management-display-ads.js");\r\nconst { getParamInWindow, postDataToParent, getDataBanner } = __webpack_require__(/*! ./util */ "./assets/js/util.js");\r\n\r\nconst tid = setInterval( function () {\r\n  if ( document.readyState !== \'complete\' ) return;\r\n  clearInterval(tid);\r\n  clearTimeout(maxTimeoutInterval)\r\n  const zoneId = getParamInWindow(_va_constant.PARAM_URL_IFRAME.ZONE_ID);\r\n  const clientId = getParamInWindow(_va_constant.PARAM_URL_IFRAME.CLIENT_ID);\r\n  const dataFullWidthResponsive = getParamInWindow(_va_constant.PARAM_URL_IFRAME.DATA_FULL_WIDTH_RESPONSE);\r\n\r\n  // const zoneFake = {\r\n  //   width: 350,\r\n  //   height: 400,\r\n  //   layout: {\r\n  //     name: "",\r\n  //     layout_type: "layout_img_only",\r\n  //     style_component: {\r\n  //       "background-color": "#FFFFFFFF",\r\n  //       "border-color": "#00000000"\r\n  //     },\r\n  //     style_image: {\r\n  //       // aspect_ratio: "1.91:1"\r\n  //     },\r\n  //     style_title: {\r\n  //       "font-family": "Roboto",\r\n  //       "font-size": "14px",\r\n  //       "text-align": "left",\r\n  //       "font-style": "normal",\r\n  //       "font-weight": "auto",\r\n  //       color: "#000"\r\n  //     },\r\n  //     style_description: {\r\n  //       "font-family": "Roboto",\r\n  //       "font-size": "14px",\r\n  //       "text-align": "left",\r\n  //       "font-style": "normal",\r\n  //       "font-weight": "auto",\r\n  //       color: "#000"\r\n  //     }\r\n  //   }\r\n  // }\r\n  //\r\n  // const bannerFake = [{\r\n  //   id: 113,\r\n  //   title: \'Khóa học lập trình web đi làm\',\r\n  //   description: \'Học online bất kỳ đâu, được kèm cặp qua nhóm kín 24/24. Đi sâu vào dự án thực tế để đi làm\',\r\n  //   media: [\r\n  //     {\r\n  //       type: \'landscape\',\r\n  //       data: \'https://cdn.noron.vn/2021/12/15/5ade151c-d53d-4c4a-a248-5317ab9c2e50-1639562331.mp4\'\r\n  //     },\r\n  //     {\r\n  //       type: \'square\',\r\n  //       data: \'https://cdn.noron.vn/2021/12/15/5ade151c-d53d-4c4a-a248-5317ab9c2e50-1639562331.mp4\'\r\n  //     }\r\n  //   ]\r\n  // }]\r\n  //\r\n  // managementDisplayAds.init({\r\n  //   idDisplayAds: \'show-display-ads\',\r\n  //   zone: zoneFake,\r\n  //   banners: bannerFake,\r\n  //   dataFullWidthResponsive: dataFullWidthResponsive === "true" || !!dataFullWidthResponsive\r\n  // })\r\n\r\n  getDataBanner(zoneId, { clientId },\r\n    ({ banners, zone } = {}) => {\r\n      if (zone && banners && banners.length > 0) {\r\n        managementDisplayAds.init({\r\n          idDisplayAds: \'show-display-ads\',\r\n          zone,\r\n          banners,\r\n          dataFullWidthResponsive: dataFullWidthResponsive === "true"\r\n        })\r\n      } else {\r\n        postDataToParent(_va_constant.POST_MESSAGE.EVENT_RESIZE_ZONE, {\r\n          width: 0,\r\n          height: 0\r\n        });\r\n      }\r\n    }, () => {\r\n      postDataToParent(_va_constant.POST_MESSAGE.EVENT_RESIZE_ZONE, {\r\n        width: 0,\r\n        height: 0\r\n      });\r\n    });\r\n}, 100);\r\n\r\nconst maxTimeoutInterval = setTimeout(() => {\r\n  clearInterval(tid);\r\n  postDataToParent(_va_constant.POST_MESSAGE.EVENT_RESIZE_ZONE, {\r\n    width: 0,\r\n    height: 0\r\n  });\r\n}, 60*1000)\r\n\n\n//# sourceURL=webpack:///./assets/js/show-ads.js?')},"./assets/js/typeof.js":module=>{eval("const _va_typeof = {\r\n  isObject(value) {\r\n    return value && typeof value === 'object' && value.constructor === Object;\r\n  },\r\n\r\n  isArray(value) {\r\n    return value && typeof value === 'object' && value.constructor === Array;\r\n  },\r\n\r\n  isString(value) {\r\n    return typeof value === 'string' || value instanceof String;\r\n  },\r\n\r\n  isEmpty(value) {\r\n    if (this.isString(value)) {\r\n      return value === '' || value === undefined || value === null;\r\n    } else if (this.isObject(value)) {\r\n      return value === null || Object.keys(value).length === 0;\r\n    } else if (this.isArray(value)) {\r\n      return value === null || value.length === 0;\r\n    }\r\n    return value === undefined || value === null;\r\n  },\r\n\r\n  isNumber(value) {\r\n    // eslint-disable-next-line no-restricted-globals\r\n    return typeof value === 'number' && this.isFinite(value);\r\n  },\r\n\r\n  isNumberStr(value) {\r\n    // eslint-disable-next-line no-restricted-globals\r\n    return !this.isNaN(value);\r\n  },\r\n\r\n  isFunction(value) {\r\n    return typeof value === \"function\";\r\n  },\r\n\r\n// Returns if a value is null\r\n  isNull(value) {\r\n    return value === null;\r\n  },\r\n\r\n// Returns if a value is undefined\r\n  isUndefined(value) {\r\n    return typeof value === 'undefined';\r\n  },\r\n\r\n  isBoolean(value) {\r\n    return typeof value === 'boolean';\r\n  },\r\n\r\n  isRegExp(value) {\r\n    return value && typeof value === 'object' && value.constructor === RegExp;\r\n  },\r\n\r\n  isError(value) {\r\n    return value instanceof Error && typeof value.message !== 'undefined';\r\n  },\r\n  isDate(value) {\r\n    return value instanceof Date;\r\n  },\r\n  isSymbol(value) {\r\n    return typeof value === 'symbol';\r\n  },\r\n  isExistKey(object, key) {\r\n    // eslint-disable-next-line no-prototype-builtins\r\n    return this.isObject(object) && object.hasOwnProperty(key);\r\n  },\r\n  isEmptyObject(object) {\r\n    // eslint-disable-next-line no-prototype-builtins\r\n    return this.isObject(object) && Object.keys(object).length === 0;\r\n  },\r\n  isErrorResponse(response) {\r\n    return !(!this.isExistKey(response, 'code') || response.code === 0);\r\n  },\r\n  isAsync(value) {\r\n    return this.isFunction(value) && value.constructor.name === 'AsyncFunction';\r\n  },\r\n  isPromise(value) {\r\n    return value instanceof Promise;\r\n  },\r\n  isBase64(data) {\r\n    try {\r\n      return /^data:((?:\\w+\\/(?:(?!;).)+)?)((?:;[\\w\\W]*?[^;])*),(.+)|^,[a-zA-Z0-9\\/+]*$/.test(data);\r\n    } catch (e) {\r\n      return false;\r\n    }\r\n  },\r\n  isUrl(text) {\r\n    if (text) {\r\n      try {\r\n        const urlReg = /^https?:\\/\\/(www\\.)?[-a-zA-Z0-9@:%._\\+~#=]{1,256}\\.[a-zA-Z0-9()]{1,6}\\b([-a-zA-Z0-9()@:%_\\+.~#?&//=]*)/g;\r\n\r\n        const links = text.trim().replace(String.fromCharCode(160), '\\n').match(urlReg);\r\n        return !!links;\r\n      } catch (e) {\r\n        return false;\r\n      }\r\n    }\r\n    return false;\r\n  }\r\n}\r\n\r\nmodule.exports = _va_typeof;\r\n\n\n//# sourceURL=webpack:///./assets/js/typeof.js?")},"./assets/js/util.js":(module,__unused_webpack_exports,__webpack_require__)=>{eval("const _va_typeof = __webpack_require__(/*! ./typeof */ \"./assets/js/typeof.js\");\r\nconst _va_constant = __webpack_require__(/*! ./constant */ \"./assets/js/constant.js\");\r\nconst { _instanceAxiosDisplayAdsCore } = __webpack_require__(/*! ./axios-config */ \"./assets/js/axios-config.js\");\r\n\r\nfunction postDataToParent(event, payload) {\r\n  const zoneId = getParamInWindow(_va_constant.PARAM_URL_IFRAME.ZONE_ID);\r\n  const adsElId = getParamInWindow(_va_constant.PARAM_URL_IFRAME.ADS_EL_ID);\r\n\r\n  if (window.parent) {\r\n    const data = {\r\n      key: compressKeyMessage(zoneId, adsElId),\r\n      source: _va_constant.POST_MESSAGE.SOURCE,\r\n      payload: {\r\n        event: event,\r\n        payload\r\n      }\r\n    }\r\n    const parent = window.parent\r\n    parent.postMessage(data, \"*\");\r\n  }\r\n}\r\n\r\nfunction wrap(toWrap, wrapper) {\r\n  wrapper = wrapper || document.createElement('div');\r\n  toWrap.parentNode.insertBefore(wrapper, toWrap);\r\n  return wrapper.appendChild(toWrap);\r\n}\r\n\r\nfunction createElementFromHTML(htmlString) {\r\n  const div = document.createElement('div');\r\n  div.innerHTML = htmlString.trim();\r\n\r\n  // Change this to div.childNodes to support multiple top-level nodes.\r\n  return div.firstChild;\r\n}\r\n\r\nfunction addListener(selector, event, callback) {\r\n  setTimeout(() => {\r\n    const el = document.querySelector(selector);\r\n    if (el) {\r\n      el.addEventListener(event, callback)\r\n    }\r\n  }, 0)\r\n}\r\n\r\nfunction getParamInWindow(name, url = window.location.href) {\r\n  name = name.replace(/[\\[\\]]/g, '\\\\$&');\r\n  const regex = new RegExp('[?&]' + name + '(=([^&#]*)|&|#|$)'),\r\n    results = regex.exec(url);\r\n  if (!results) return null;\r\n  if (!results[2]) return '';\r\n  return decodeURIComponent(results[2].replace(/\\+/g, ' '));\r\n}\r\n\r\nfunction getDataBanner(zoneId, options = {}, callback, errorCallback) {\r\n\r\n  if (zoneId) {\r\n    if (_va_constant.SERVER_CONFIG.MOCK_API) {\r\n      callback(_va_constant.DATA_MOCK.ZONE_DISPLAY[zoneId])\r\n      return;\r\n    }\r\n\r\n    const {\r\n      clientId\r\n    } = options;\r\n\r\n    _instanceAxiosDisplayAdsCore\r\n      .get('/banners', {\r\n        params: {\r\n          client_id: clientId,\r\n          zone_id: zoneId\r\n        }\r\n      }).then(function (response) {\r\n        if (response.status === 200 && callback) {\r\n          const {\r\n            code,\r\n            message,\r\n            body\r\n          } = response.data || {};\r\n          if (body && (code === 200 || code === 0)) {\r\n            callback(body);\r\n          }\r\n        } else if (errorCallback) {\r\n          errorCallback(response);\r\n        }\r\n      })\r\n      .catch(function (error) {\r\n        console.log(error);\r\n        if (errorCallback) {\r\n          errorCallback(error);\r\n        }\r\n      }).then(function () {\r\n      // always executed\r\n    });\r\n  }\r\n}\r\n\r\nfunction getHiddenWindow () {\r\n  const list_hidden = ['hidden', 'mozHidden', 'webkitHidden', 'msHidden'];\r\n  return list_hidden.find(hidden => hidden in document);\r\n}\r\n\r\nconst addEventVisibleWindow = (eventListener) => {\r\n  switch (getHiddenWindow()) {\r\n    case 'hidden':\r\n      document.addEventListener('visibilitychange', eventListener);\r\n      break;\r\n    case 'mozHidden':\r\n      document.addEventListener('mozvisibilitychange', eventListener);\r\n      break;\r\n    case 'webkitHidden':\r\n      document.addEventListener('webkitvisibilitychange', eventListener);\r\n      break;\r\n    case 'msHidden':\r\n      document.addEventListener('msvisibilitychange', eventListener);\r\n      break;\r\n    default:\r\n      if ('onfocusin' in document) {\r\n        document.addEventListener('focusin', eventListener);\r\n        document.addEventListener('focusout', eventListener);\r\n      } else {\r\n        window.addEventListener('pageshow', eventListener);\r\n        window.addEventListener('pagehide', eventListener);\r\n        window.addEventListener('focus', eventListener);\r\n        window.addEventListener('blur', eventListener);\r\n      }\r\n  }\r\n}\r\n\r\nconst removeEventVisibleWindow = (eventListener) => {\r\n  switch (getHiddenWindow()) {\r\n    case 'hidden':\r\n      document.removeEventListener('visibilitychange', eventListener);\r\n      break;\r\n    case 'mozHidden':\r\n      document.removeEventListener('mozvisibilitychange', eventListener);\r\n      break;\r\n    case 'webkitHidden':\r\n      document.removeEventListener('webkitvisibilitychange', eventListener);\r\n      break;\r\n    case 'msHidden':\r\n      document.removeEventListener('msvisibilitychange', eventListener);\r\n      break;\r\n    default:\r\n      if ('onfocusin' in document) {\r\n        document.removeEventListener('focusin', eventListener);\r\n        document.removeEventListener('focusout', eventListener);\r\n      } else {\r\n        window.removeEventListener('pageshow', eventListener);\r\n        window.removeEventListener('pagehide', eventListener);\r\n        window.removeEventListener('focus', eventListener);\r\n        window.removeEventListener('blur', eventListener);\r\n      }\r\n  }\r\n}\r\n\r\nconst thresholdArray = steps => Array(steps + 1)\r\n  .fill(0)\r\n  .map((_, index) => index / steps || 0)\r\n\r\nconst parseGETParams = ({ baseURI, params }) => {\r\n  let resultURI = baseURI;\r\n  let gotValidParam = false;\r\n  if (params) {\r\n    Object.keys(params).forEach(keyName => {\r\n      if (!_va_typeof.isEmpty(params[keyName])) {\r\n        if (_va_typeof.isArray(params[keyName])) {\r\n          params[keyName].forEach(v => {\r\n            if (gotValidParam) {\r\n              resultURI += `&${keyName}[]=${v}`;\r\n            } else {\r\n              resultURI += `?${keyName}[]=${v}`;\r\n              gotValidParam = true;\r\n            }\r\n          });\r\n        } else {\r\n          if (gotValidParam) {\r\n            resultURI += `&${keyName}=${params[keyName]}`;\r\n          } else {\r\n            resultURI += `?${keyName}=${params[keyName]}`;\r\n            gotValidParam = true;\r\n          }\r\n        }\r\n      }\r\n    });\r\n    return resultURI;\r\n  }\r\n  return resultURI;\r\n};\r\n\r\nconst pushParams = ({ baseURI, params }) => {\r\n  if (!baseURI || !params) return baseURI;\r\n  let resultURI = baseURI;\r\n  if (baseURI.indexOf('?') === -1) {\r\n    return parseGETParams({ baseURI, params });\r\n  }\r\n\r\n  Object.keys(params).forEach(keyName => {\r\n    if (!_va_typeof.isEmpty(params[keyName])) {\r\n      if (_va_typeof.isArray(params[keyName])) {\r\n        params[keyName].forEach(v => {\r\n          resultURI += `&${keyName}[]=${v}`;\r\n        });\r\n      } else {\r\n        resultURI += `&${keyName}=${params[keyName]}`;\r\n      }\r\n    }\r\n  });\r\n  return resultURI;\r\n};\r\n\r\nfunction getVisibleByEvent (evt) {\r\n  if (!evt) return false;\r\n  const hidden = getHiddenWindow() || 'hidden';\r\n  let v = 'visible';\r\n  let h = 'hidden';\r\n\r\n  let evtMap = {\r\n    focus: v, focusin: v, pageshow: v, blur: h, focusout: h, pagehide: h\r\n  };\r\n\r\n  if (evt.type in evtMap) {\r\n    return evtMap[evt.type] === v;\r\n  } else {\r\n    return !document[hidden];\r\n  }\r\n}\r\n\r\nfunction compressKeyMessage(zoneId, elementId) {\r\n  return `${zoneId}${_va_constant.KEY_MESSAGE_MERGE}${elementId}`\r\n}\r\n\r\nfunction extractByKeyMessage(key) {\r\n  if (key) {\r\n    const [zoneId, elementId] = key.split(_va_constant.KEY_MESSAGE_MERGE);\r\n    return {\r\n      zoneId,\r\n      elementId\r\n    }\r\n  }\r\n\r\n  return {\r\n    zoneId: null,\r\n    elementId: null\r\n  };\r\n}\r\n\r\nfunction getScale(width, height) {\r\n  if (width === 0 || height === 0 || width === null || height === null) {\r\n    return 0;\r\n  }\r\n\r\n  let scale = 1;\r\n  try {\r\n    scale = height * 100/width;\r\n  } catch (e) {\r\n    console.log(e);\r\n  }\r\n\r\n  return scale;\r\n}\r\n\r\nfunction resizeByScale(element, scale) {\r\n  if (element && element.parentNode) {\r\n    const parent = element.parentNode;\r\n\r\n    parent.style.width = `100%`;\r\n    parent.style.height = `0`;\r\n    parent.style.paddingTop = `${scale}%`;\r\n    parent.style.paddingBottom = `0`;\r\n    parent.style.margin = 'auto';\r\n    parent.style.position = 'relative';\r\n\r\n    element.style.width = `100%`;\r\n    element.style.height = `100%`;\r\n    element.style.margin = 'auto';\r\n    element.style.top = '0';\r\n    element.style.bottom = '0';\r\n    element.style.left = '0';\r\n    element.style.right = '0';\r\n    element.style.position = 'absolute';\r\n  }\r\n}\r\n\r\nfunction convertObjectToStyle(object) {\r\n  if (object) {\r\n    return Object.entries(object).map(([key, value]) => {\r\n      const propertyRequireSuffix = _va_constant.UNIT_PROPERTY_SUFFIX.some(property => key.includes(property));\r\n      if (propertyRequireSuffix && !isNaN(value)) {\r\n        return `${key}: ${value}${_va_constant.PROPERTY_SUFFIX_DEFAULT}`;\r\n      }\r\n      return `${key}: ${value}`;\r\n    }).join(';')\r\n  }\r\n  return '';\r\n}\r\n\r\nfunction extractWidth(str) {\r\n  try {\r\n    const [, number, unit] = str.match(/([0-9]*)([%px]*)/i);\r\n    return {\r\n      value: Number.parseFloat(number),\r\n      unit: unit\r\n    };\r\n  } catch (e) {\r\n    console.log(e);\r\n  }\r\n  return null;\r\n}\r\n\r\nmodule.exports = {\r\n  postDataToParent,\r\n  getParamInWindow,\r\n  getDataBanner,\r\n  getHiddenWindow,\r\n  addEventVisibleWindow,\r\n  removeEventVisibleWindow,\r\n  thresholdArray,\r\n  parseGETParams,\r\n  pushParams,\r\n  getVisibleByEvent,\r\n  extractByKeyMessage,\r\n  getScale,\r\n  resizeByScale,\r\n  convertObjectToStyle,\r\n  extractWidth,\r\n  wrap,\r\n  addListener,\r\n  createElementFromHTML\r\n}\r\n\n\n//# sourceURL=webpack:///./assets/js/util.js?")},"./node_modules/axios/dist/browser/axios.cjs":(module,__unused_webpack_exports,__webpack_require__)=>{"use strict";eval("// Axios v1.4.0 Copyright (c) 2023 Matt Zabriskie and contributors\n\n\nfunction bind(fn, thisArg) {\n  return function wrap() {\n    return fn.apply(thisArg, arguments);\n  };\n}\n\n// utils is a library of generic helper functions non-specific to axios\n\nconst {toString} = Object.prototype;\nconst {getPrototypeOf} = Object;\n\nconst kindOf = (cache => thing => {\n    const str = toString.call(thing);\n    return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());\n})(Object.create(null));\n\nconst kindOfTest = (type) => {\n  type = type.toLowerCase();\n  return (thing) => kindOf(thing) === type\n};\n\nconst typeOfTest = type => thing => typeof thing === type;\n\n/**\n * Determine if a value is an Array\n *\n * @param {Object} val The value to test\n *\n * @returns {boolean} True if value is an Array, otherwise false\n */\nconst {isArray} = Array;\n\n/**\n * Determine if a value is undefined\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if the value is undefined, otherwise false\n */\nconst isUndefined = typeOfTest('undefined');\n\n/**\n * Determine if a value is a Buffer\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a Buffer, otherwise false\n */\nfunction isBuffer(val) {\n  return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor)\n    && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);\n}\n\n/**\n * Determine if a value is an ArrayBuffer\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is an ArrayBuffer, otherwise false\n */\nconst isArrayBuffer = kindOfTest('ArrayBuffer');\n\n\n/**\n * Determine if a value is a view on an ArrayBuffer\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false\n */\nfunction isArrayBufferView(val) {\n  let result;\n  if ((typeof ArrayBuffer !== 'undefined') && (ArrayBuffer.isView)) {\n    result = ArrayBuffer.isView(val);\n  } else {\n    result = (val) && (val.buffer) && (isArrayBuffer(val.buffer));\n  }\n  return result;\n}\n\n/**\n * Determine if a value is a String\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a String, otherwise false\n */\nconst isString = typeOfTest('string');\n\n/**\n * Determine if a value is a Function\n *\n * @param {*} val The value to test\n * @returns {boolean} True if value is a Function, otherwise false\n */\nconst isFunction = typeOfTest('function');\n\n/**\n * Determine if a value is a Number\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a Number, otherwise false\n */\nconst isNumber = typeOfTest('number');\n\n/**\n * Determine if a value is an Object\n *\n * @param {*} thing The value to test\n *\n * @returns {boolean} True if value is an Object, otherwise false\n */\nconst isObject = (thing) => thing !== null && typeof thing === 'object';\n\n/**\n * Determine if a value is a Boolean\n *\n * @param {*} thing The value to test\n * @returns {boolean} True if value is a Boolean, otherwise false\n */\nconst isBoolean = thing => thing === true || thing === false;\n\n/**\n * Determine if a value is a plain Object\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a plain Object, otherwise false\n */\nconst isPlainObject = (val) => {\n  if (kindOf(val) !== 'object') {\n    return false;\n  }\n\n  const prototype = getPrototypeOf(val);\n  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in val) && !(Symbol.iterator in val);\n};\n\n/**\n * Determine if a value is a Date\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a Date, otherwise false\n */\nconst isDate = kindOfTest('Date');\n\n/**\n * Determine if a value is a File\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a File, otherwise false\n */\nconst isFile = kindOfTest('File');\n\n/**\n * Determine if a value is a Blob\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a Blob, otherwise false\n */\nconst isBlob = kindOfTest('Blob');\n\n/**\n * Determine if a value is a FileList\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a File, otherwise false\n */\nconst isFileList = kindOfTest('FileList');\n\n/**\n * Determine if a value is a Stream\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a Stream, otherwise false\n */\nconst isStream = (val) => isObject(val) && isFunction(val.pipe);\n\n/**\n * Determine if a value is a FormData\n *\n * @param {*} thing The value to test\n *\n * @returns {boolean} True if value is an FormData, otherwise false\n */\nconst isFormData = (thing) => {\n  let kind;\n  return thing && (\n    (typeof FormData === 'function' && thing instanceof FormData) || (\n      isFunction(thing.append) && (\n        (kind = kindOf(thing)) === 'formdata' ||\n        // detect form-data instance\n        (kind === 'object' && isFunction(thing.toString) && thing.toString() === '[object FormData]')\n      )\n    )\n  )\n};\n\n/**\n * Determine if a value is a URLSearchParams object\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a URLSearchParams object, otherwise false\n */\nconst isURLSearchParams = kindOfTest('URLSearchParams');\n\n/**\n * Trim excess whitespace off the beginning and end of a string\n *\n * @param {String} str The String to trim\n *\n * @returns {String} The String freed of excess whitespace\n */\nconst trim = (str) => str.trim ?\n  str.trim() : str.replace(/^[\\s\\uFEFF\\xA0]+|[\\s\\uFEFF\\xA0]+$/g, '');\n\n/**\n * Iterate over an Array or an Object invoking a function for each item.\n *\n * If `obj` is an Array callback will be called passing\n * the value, index, and complete array for each item.\n *\n * If 'obj' is an Object callback will be called passing\n * the value, key, and complete object for each property.\n *\n * @param {Object|Array} obj The object to iterate\n * @param {Function} fn The callback to invoke for each item\n *\n * @param {Boolean} [allOwnKeys = false]\n * @returns {any}\n */\nfunction forEach(obj, fn, {allOwnKeys = false} = {}) {\n  // Don't bother if no value provided\n  if (obj === null || typeof obj === 'undefined') {\n    return;\n  }\n\n  let i;\n  let l;\n\n  // Force an array if not already something iterable\n  if (typeof obj !== 'object') {\n    /*eslint no-param-reassign:0*/\n    obj = [obj];\n  }\n\n  if (isArray(obj)) {\n    // Iterate over array values\n    for (i = 0, l = obj.length; i < l; i++) {\n      fn.call(null, obj[i], i, obj);\n    }\n  } else {\n    // Iterate over object keys\n    const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);\n    const len = keys.length;\n    let key;\n\n    for (i = 0; i < len; i++) {\n      key = keys[i];\n      fn.call(null, obj[key], key, obj);\n    }\n  }\n}\n\nfunction findKey(obj, key) {\n  key = key.toLowerCase();\n  const keys = Object.keys(obj);\n  let i = keys.length;\n  let _key;\n  while (i-- > 0) {\n    _key = keys[i];\n    if (key === _key.toLowerCase()) {\n      return _key;\n    }\n  }\n  return null;\n}\n\nconst _global = (() => {\n  /*eslint no-undef:0*/\n  if (typeof globalThis !== \"undefined\") return globalThis;\n  return typeof self !== \"undefined\" ? self : (typeof window !== 'undefined' ? window : __webpack_require__.g)\n})();\n\nconst isContextDefined = (context) => !isUndefined(context) && context !== _global;\n\n/**\n * Accepts varargs expecting each argument to be an object, then\n * immutably merges the properties of each object and returns result.\n *\n * When multiple objects contain the same key the later object in\n * the arguments list will take precedence.\n *\n * Example:\n *\n * ```js\n * var result = merge({foo: 123}, {foo: 456});\n * console.log(result.foo); // outputs 456\n * ```\n *\n * @param {Object} obj1 Object to merge\n *\n * @returns {Object} Result of all merge properties\n */\nfunction merge(/* obj1, obj2, obj3, ... */) {\n  const {caseless} = isContextDefined(this) && this || {};\n  const result = {};\n  const assignValue = (val, key) => {\n    const targetKey = caseless && findKey(result, key) || key;\n    if (isPlainObject(result[targetKey]) && isPlainObject(val)) {\n      result[targetKey] = merge(result[targetKey], val);\n    } else if (isPlainObject(val)) {\n      result[targetKey] = merge({}, val);\n    } else if (isArray(val)) {\n      result[targetKey] = val.slice();\n    } else {\n      result[targetKey] = val;\n    }\n  };\n\n  for (let i = 0, l = arguments.length; i < l; i++) {\n    arguments[i] && forEach(arguments[i], assignValue);\n  }\n  return result;\n}\n\n/**\n * Extends object a by mutably adding to it the properties of object b.\n *\n * @param {Object} a The object to be extended\n * @param {Object} b The object to copy properties from\n * @param {Object} thisArg The object to bind function to\n *\n * @param {Boolean} [allOwnKeys]\n * @returns {Object} The resulting value of object a\n */\nconst extend = (a, b, thisArg, {allOwnKeys}= {}) => {\n  forEach(b, (val, key) => {\n    if (thisArg && isFunction(val)) {\n      a[key] = bind(val, thisArg);\n    } else {\n      a[key] = val;\n    }\n  }, {allOwnKeys});\n  return a;\n};\n\n/**\n * Remove byte order marker. This catches EF BB BF (the UTF-8 BOM)\n *\n * @param {string} content with BOM\n *\n * @returns {string} content value without BOM\n */\nconst stripBOM = (content) => {\n  if (content.charCodeAt(0) === 0xFEFF) {\n    content = content.slice(1);\n  }\n  return content;\n};\n\n/**\n * Inherit the prototype methods from one constructor into another\n * @param {function} constructor\n * @param {function} superConstructor\n * @param {object} [props]\n * @param {object} [descriptors]\n *\n * @returns {void}\n */\nconst inherits = (constructor, superConstructor, props, descriptors) => {\n  constructor.prototype = Object.create(superConstructor.prototype, descriptors);\n  constructor.prototype.constructor = constructor;\n  Object.defineProperty(constructor, 'super', {\n    value: superConstructor.prototype\n  });\n  props && Object.assign(constructor.prototype, props);\n};\n\n/**\n * Resolve object with deep prototype chain to a flat object\n * @param {Object} sourceObj source object\n * @param {Object} [destObj]\n * @param {Function|Boolean} [filter]\n * @param {Function} [propFilter]\n *\n * @returns {Object}\n */\nconst toFlatObject = (sourceObj, destObj, filter, propFilter) => {\n  let props;\n  let i;\n  let prop;\n  const merged = {};\n\n  destObj = destObj || {};\n  // eslint-disable-next-line no-eq-null,eqeqeq\n  if (sourceObj == null) return destObj;\n\n  do {\n    props = Object.getOwnPropertyNames(sourceObj);\n    i = props.length;\n    while (i-- > 0) {\n      prop = props[i];\n      if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {\n        destObj[prop] = sourceObj[prop];\n        merged[prop] = true;\n      }\n    }\n    sourceObj = filter !== false && getPrototypeOf(sourceObj);\n  } while (sourceObj && (!filter || filter(sourceObj, destObj)) && sourceObj !== Object.prototype);\n\n  return destObj;\n};\n\n/**\n * Determines whether a string ends with the characters of a specified string\n *\n * @param {String} str\n * @param {String} searchString\n * @param {Number} [position= 0]\n *\n * @returns {boolean}\n */\nconst endsWith = (str, searchString, position) => {\n  str = String(str);\n  if (position === undefined || position > str.length) {\n    position = str.length;\n  }\n  position -= searchString.length;\n  const lastIndex = str.indexOf(searchString, position);\n  return lastIndex !== -1 && lastIndex === position;\n};\n\n\n/**\n * Returns new array from array like object or null if failed\n *\n * @param {*} [thing]\n *\n * @returns {?Array}\n */\nconst toArray = (thing) => {\n  if (!thing) return null;\n  if (isArray(thing)) return thing;\n  let i = thing.length;\n  if (!isNumber(i)) return null;\n  const arr = new Array(i);\n  while (i-- > 0) {\n    arr[i] = thing[i];\n  }\n  return arr;\n};\n\n/**\n * Checking if the Uint8Array exists and if it does, it returns a function that checks if the\n * thing passed in is an instance of Uint8Array\n *\n * @param {TypedArray}\n *\n * @returns {Array}\n */\n// eslint-disable-next-line func-names\nconst isTypedArray = (TypedArray => {\n  // eslint-disable-next-line func-names\n  return thing => {\n    return TypedArray && thing instanceof TypedArray;\n  };\n})(typeof Uint8Array !== 'undefined' && getPrototypeOf(Uint8Array));\n\n/**\n * For each entry in the object, call the function with the key and value.\n *\n * @param {Object<any, any>} obj - The object to iterate over.\n * @param {Function} fn - The function to call for each entry.\n *\n * @returns {void}\n */\nconst forEachEntry = (obj, fn) => {\n  const generator = obj && obj[Symbol.iterator];\n\n  const iterator = generator.call(obj);\n\n  let result;\n\n  while ((result = iterator.next()) && !result.done) {\n    const pair = result.value;\n    fn.call(obj, pair[0], pair[1]);\n  }\n};\n\n/**\n * It takes a regular expression and a string, and returns an array of all the matches\n *\n * @param {string} regExp - The regular expression to match against.\n * @param {string} str - The string to search.\n *\n * @returns {Array<boolean>}\n */\nconst matchAll = (regExp, str) => {\n  let matches;\n  const arr = [];\n\n  while ((matches = regExp.exec(str)) !== null) {\n    arr.push(matches);\n  }\n\n  return arr;\n};\n\n/* Checking if the kindOfTest function returns true when passed an HTMLFormElement. */\nconst isHTMLForm = kindOfTest('HTMLFormElement');\n\nconst toCamelCase = str => {\n  return str.toLowerCase().replace(/[-_\\s]([a-z\\d])(\\w*)/g,\n    function replacer(m, p1, p2) {\n      return p1.toUpperCase() + p2;\n    }\n  );\n};\n\n/* Creating a function that will check if an object has a property. */\nconst hasOwnProperty = (({hasOwnProperty}) => (obj, prop) => hasOwnProperty.call(obj, prop))(Object.prototype);\n\n/**\n * Determine if a value is a RegExp object\n *\n * @param {*} val The value to test\n *\n * @returns {boolean} True if value is a RegExp object, otherwise false\n */\nconst isRegExp = kindOfTest('RegExp');\n\nconst reduceDescriptors = (obj, reducer) => {\n  const descriptors = Object.getOwnPropertyDescriptors(obj);\n  const reducedDescriptors = {};\n\n  forEach(descriptors, (descriptor, name) => {\n    if (reducer(descriptor, name, obj) !== false) {\n      reducedDescriptors[name] = descriptor;\n    }\n  });\n\n  Object.defineProperties(obj, reducedDescriptors);\n};\n\n/**\n * Makes all methods read-only\n * @param {Object} obj\n */\n\nconst freezeMethods = (obj) => {\n  reduceDescriptors(obj, (descriptor, name) => {\n    // skip restricted props in strict mode\n    if (isFunction(obj) && ['arguments', 'caller', 'callee'].indexOf(name) !== -1) {\n      return false;\n    }\n\n    const value = obj[name];\n\n    if (!isFunction(value)) return;\n\n    descriptor.enumerable = false;\n\n    if ('writable' in descriptor) {\n      descriptor.writable = false;\n      return;\n    }\n\n    if (!descriptor.set) {\n      descriptor.set = () => {\n        throw Error('Can not rewrite read-only method \\'' + name + '\\'');\n      };\n    }\n  });\n};\n\nconst toObjectSet = (arrayOrString, delimiter) => {\n  const obj = {};\n\n  const define = (arr) => {\n    arr.forEach(value => {\n      obj[value] = true;\n    });\n  };\n\n  isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));\n\n  return obj;\n};\n\nconst noop = () => {};\n\nconst toFiniteNumber = (value, defaultValue) => {\n  value = +value;\n  return Number.isFinite(value) ? value : defaultValue;\n};\n\nconst ALPHA = 'abcdefghijklmnopqrstuvwxyz';\n\nconst DIGIT = '0123456789';\n\nconst ALPHABET = {\n  DIGIT,\n  ALPHA,\n  ALPHA_DIGIT: ALPHA + ALPHA.toUpperCase() + DIGIT\n};\n\nconst generateString = (size = 16, alphabet = ALPHABET.ALPHA_DIGIT) => {\n  let str = '';\n  const {length} = alphabet;\n  while (size--) {\n    str += alphabet[Math.random() * length|0];\n  }\n\n  return str;\n};\n\n/**\n * If the thing is a FormData object, return true, otherwise return false.\n *\n * @param {unknown} thing - The thing to check.\n *\n * @returns {boolean}\n */\nfunction isSpecCompliantForm(thing) {\n  return !!(thing && isFunction(thing.append) && thing[Symbol.toStringTag] === 'FormData' && thing[Symbol.iterator]);\n}\n\nconst toJSONObject = (obj) => {\n  const stack = new Array(10);\n\n  const visit = (source, i) => {\n\n    if (isObject(source)) {\n      if (stack.indexOf(source) >= 0) {\n        return;\n      }\n\n      if(!('toJSON' in source)) {\n        stack[i] = source;\n        const target = isArray(source) ? [] : {};\n\n        forEach(source, (value, key) => {\n          const reducedValue = visit(value, i + 1);\n          !isUndefined(reducedValue) && (target[key] = reducedValue);\n        });\n\n        stack[i] = undefined;\n\n        return target;\n      }\n    }\n\n    return source;\n  };\n\n  return visit(obj, 0);\n};\n\nconst isAsyncFn = kindOfTest('AsyncFunction');\n\nconst isThenable = (thing) =>\n  thing && (isObject(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);\n\nvar utils = {\n  isArray,\n  isArrayBuffer,\n  isBuffer,\n  isFormData,\n  isArrayBufferView,\n  isString,\n  isNumber,\n  isBoolean,\n  isObject,\n  isPlainObject,\n  isUndefined,\n  isDate,\n  isFile,\n  isBlob,\n  isRegExp,\n  isFunction,\n  isStream,\n  isURLSearchParams,\n  isTypedArray,\n  isFileList,\n  forEach,\n  merge,\n  extend,\n  trim,\n  stripBOM,\n  inherits,\n  toFlatObject,\n  kindOf,\n  kindOfTest,\n  endsWith,\n  toArray,\n  forEachEntry,\n  matchAll,\n  isHTMLForm,\n  hasOwnProperty,\n  hasOwnProp: hasOwnProperty, // an alias to avoid ESLint no-prototype-builtins detection\n  reduceDescriptors,\n  freezeMethods,\n  toObjectSet,\n  toCamelCase,\n  noop,\n  toFiniteNumber,\n  findKey,\n  global: _global,\n  isContextDefined,\n  ALPHABET,\n  generateString,\n  isSpecCompliantForm,\n  toJSONObject,\n  isAsyncFn,\n  isThenable\n};\n\n/**\n * Create an Error with the specified message, config, error code, request and response.\n *\n * @param {string} message The error message.\n * @param {string} [code] The error code (for example, 'ECONNABORTED').\n * @param {Object} [config] The config.\n * @param {Object} [request] The request.\n * @param {Object} [response] The response.\n *\n * @returns {Error} The created error.\n */\nfunction AxiosError(message, code, config, request, response) {\n  Error.call(this);\n\n  if (Error.captureStackTrace) {\n    Error.captureStackTrace(this, this.constructor);\n  } else {\n    this.stack = (new Error()).stack;\n  }\n\n  this.message = message;\n  this.name = 'AxiosError';\n  code && (this.code = code);\n  config && (this.config = config);\n  request && (this.request = request);\n  response && (this.response = response);\n}\n\nutils.inherits(AxiosError, Error, {\n  toJSON: function toJSON() {\n    return {\n      // Standard\n      message: this.message,\n      name: this.name,\n      // Microsoft\n      description: this.description,\n      number: this.number,\n      // Mozilla\n      fileName: this.fileName,\n      lineNumber: this.lineNumber,\n      columnNumber: this.columnNumber,\n      stack: this.stack,\n      // Axios\n      config: utils.toJSONObject(this.config),\n      code: this.code,\n      status: this.response && this.response.status ? this.response.status : null\n    };\n  }\n});\n\nconst prototype$1 = AxiosError.prototype;\nconst descriptors = {};\n\n[\n  'ERR_BAD_OPTION_VALUE',\n  'ERR_BAD_OPTION',\n  'ECONNABORTED',\n  'ETIMEDOUT',\n  'ERR_NETWORK',\n  'ERR_FR_TOO_MANY_REDIRECTS',\n  'ERR_DEPRECATED',\n  'ERR_BAD_RESPONSE',\n  'ERR_BAD_REQUEST',\n  'ERR_CANCELED',\n  'ERR_NOT_SUPPORT',\n  'ERR_INVALID_URL'\n// eslint-disable-next-line func-names\n].forEach(code => {\n  descriptors[code] = {value: code};\n});\n\nObject.defineProperties(AxiosError, descriptors);\nObject.defineProperty(prototype$1, 'isAxiosError', {value: true});\n\n// eslint-disable-next-line func-names\nAxiosError.from = (error, code, config, request, response, customProps) => {\n  const axiosError = Object.create(prototype$1);\n\n  utils.toFlatObject(error, axiosError, function filter(obj) {\n    return obj !== Error.prototype;\n  }, prop => {\n    return prop !== 'isAxiosError';\n  });\n\n  AxiosError.call(axiosError, error.message, code, config, request, response);\n\n  axiosError.cause = error;\n\n  axiosError.name = error.name;\n\n  customProps && Object.assign(axiosError, customProps);\n\n  return axiosError;\n};\n\n// eslint-disable-next-line strict\nvar httpAdapter = null;\n\n/**\n * Determines if the given thing is a array or js object.\n *\n * @param {string} thing - The object or array to be visited.\n *\n * @returns {boolean}\n */\nfunction isVisitable(thing) {\n  return utils.isPlainObject(thing) || utils.isArray(thing);\n}\n\n/**\n * It removes the brackets from the end of a string\n *\n * @param {string} key - The key of the parameter.\n *\n * @returns {string} the key without the brackets.\n */\nfunction removeBrackets(key) {\n  return utils.endsWith(key, '[]') ? key.slice(0, -2) : key;\n}\n\n/**\n * It takes a path, a key, and a boolean, and returns a string\n *\n * @param {string} path - The path to the current key.\n * @param {string} key - The key of the current object being iterated over.\n * @param {string} dots - If true, the key will be rendered with dots instead of brackets.\n *\n * @returns {string} The path to the current key.\n */\nfunction renderKey(path, key, dots) {\n  if (!path) return key;\n  return path.concat(key).map(function each(token, i) {\n    // eslint-disable-next-line no-param-reassign\n    token = removeBrackets(token);\n    return !dots && i ? '[' + token + ']' : token;\n  }).join(dots ? '.' : '');\n}\n\n/**\n * If the array is an array and none of its elements are visitable, then it's a flat array.\n *\n * @param {Array<any>} arr - The array to check\n *\n * @returns {boolean}\n */\nfunction isFlatArray(arr) {\n  return utils.isArray(arr) && !arr.some(isVisitable);\n}\n\nconst predicates = utils.toFlatObject(utils, {}, null, function filter(prop) {\n  return /^is[A-Z]/.test(prop);\n});\n\n/**\n * Convert a data object to FormData\n *\n * @param {Object} obj\n * @param {?Object} [formData]\n * @param {?Object} [options]\n * @param {Function} [options.visitor]\n * @param {Boolean} [options.metaTokens = true]\n * @param {Boolean} [options.dots = false]\n * @param {?Boolean} [options.indexes = false]\n *\n * @returns {Object}\n **/\n\n/**\n * It converts an object into a FormData object\n *\n * @param {Object<any, any>} obj - The object to convert to form data.\n * @param {string} formData - The FormData object to append to.\n * @param {Object<string, any>} options\n *\n * @returns\n */\nfunction toFormData(obj, formData, options) {\n  if (!utils.isObject(obj)) {\n    throw new TypeError('target must be an object');\n  }\n\n  // eslint-disable-next-line no-param-reassign\n  formData = formData || new (FormData)();\n\n  // eslint-disable-next-line no-param-reassign\n  options = utils.toFlatObject(options, {\n    metaTokens: true,\n    dots: false,\n    indexes: false\n  }, false, function defined(option, source) {\n    // eslint-disable-next-line no-eq-null,eqeqeq\n    return !utils.isUndefined(source[option]);\n  });\n\n  const metaTokens = options.metaTokens;\n  // eslint-disable-next-line no-use-before-define\n  const visitor = options.visitor || defaultVisitor;\n  const dots = options.dots;\n  const indexes = options.indexes;\n  const _Blob = options.Blob || typeof Blob !== 'undefined' && Blob;\n  const useBlob = _Blob && utils.isSpecCompliantForm(formData);\n\n  if (!utils.isFunction(visitor)) {\n    throw new TypeError('visitor must be a function');\n  }\n\n  function convertValue(value) {\n    if (value === null) return '';\n\n    if (utils.isDate(value)) {\n      return value.toISOString();\n    }\n\n    if (!useBlob && utils.isBlob(value)) {\n      throw new AxiosError('Blob is not supported. Use a Buffer instead.');\n    }\n\n    if (utils.isArrayBuffer(value) || utils.isTypedArray(value)) {\n      return useBlob && typeof Blob === 'function' ? new Blob([value]) : Buffer.from(value);\n    }\n\n    return value;\n  }\n\n  /**\n   * Default visitor.\n   *\n   * @param {*} value\n   * @param {String|Number} key\n   * @param {Array<String|Number>} path\n   * @this {FormData}\n   *\n   * @returns {boolean} return true to visit the each prop of the value recursively\n   */\n  function defaultVisitor(value, key, path) {\n    let arr = value;\n\n    if (value && !path && typeof value === 'object') {\n      if (utils.endsWith(key, '{}')) {\n        // eslint-disable-next-line no-param-reassign\n        key = metaTokens ? key : key.slice(0, -2);\n        // eslint-disable-next-line no-param-reassign\n        value = JSON.stringify(value);\n      } else if (\n        (utils.isArray(value) && isFlatArray(value)) ||\n        ((utils.isFileList(value) || utils.endsWith(key, '[]')) && (arr = utils.toArray(value))\n        )) {\n        // eslint-disable-next-line no-param-reassign\n        key = removeBrackets(key);\n\n        arr.forEach(function each(el, index) {\n          !(utils.isUndefined(el) || el === null) && formData.append(\n            // eslint-disable-next-line no-nested-ternary\n            indexes === true ? renderKey([key], index, dots) : (indexes === null ? key : key + '[]'),\n            convertValue(el)\n          );\n        });\n        return false;\n      }\n    }\n\n    if (isVisitable(value)) {\n      return true;\n    }\n\n    formData.append(renderKey(path, key, dots), convertValue(value));\n\n    return false;\n  }\n\n  const stack = [];\n\n  const exposedHelpers = Object.assign(predicates, {\n    defaultVisitor,\n    convertValue,\n    isVisitable\n  });\n\n  function build(value, path) {\n    if (utils.isUndefined(value)) return;\n\n    if (stack.indexOf(value) !== -1) {\n      throw Error('Circular reference detected in ' + path.join('.'));\n    }\n\n    stack.push(value);\n\n    utils.forEach(value, function each(el, key) {\n      const result = !(utils.isUndefined(el) || el === null) && visitor.call(\n        formData, el, utils.isString(key) ? key.trim() : key, path, exposedHelpers\n      );\n\n      if (result === true) {\n        build(el, path ? path.concat(key) : [key]);\n      }\n    });\n\n    stack.pop();\n  }\n\n  if (!utils.isObject(obj)) {\n    throw new TypeError('data must be an object');\n  }\n\n  build(obj);\n\n  return formData;\n}\n\n/**\n * It encodes a string by replacing all characters that are not in the unreserved set with\n * their percent-encoded equivalents\n *\n * @param {string} str - The string to encode.\n *\n * @returns {string} The encoded string.\n */\nfunction encode$1(str) {\n  const charMap = {\n    '!': '%21',\n    \"'\": '%27',\n    '(': '%28',\n    ')': '%29',\n    '~': '%7E',\n    '%20': '+',\n    '%00': '\\x00'\n  };\n  return encodeURIComponent(str).replace(/[!'()~]|%20|%00/g, function replacer(match) {\n    return charMap[match];\n  });\n}\n\n/**\n * It takes a params object and converts it to a FormData object\n *\n * @param {Object<string, any>} params - The parameters to be converted to a FormData object.\n * @param {Object<string, any>} options - The options object passed to the Axios constructor.\n *\n * @returns {void}\n */\nfunction AxiosURLSearchParams(params, options) {\n  this._pairs = [];\n\n  params && toFormData(params, this, options);\n}\n\nconst prototype = AxiosURLSearchParams.prototype;\n\nprototype.append = function append(name, value) {\n  this._pairs.push([name, value]);\n};\n\nprototype.toString = function toString(encoder) {\n  const _encode = encoder ? function(value) {\n    return encoder.call(this, value, encode$1);\n  } : encode$1;\n\n  return this._pairs.map(function each(pair) {\n    return _encode(pair[0]) + '=' + _encode(pair[1]);\n  }, '').join('&');\n};\n\n/**\n * It replaces all instances of the characters `:`, `$`, `,`, `+`, `[`, and `]` with their\n * URI encoded counterparts\n *\n * @param {string} val The value to be encoded.\n *\n * @returns {string} The encoded value.\n */\nfunction encode(val) {\n  return encodeURIComponent(val).\n    replace(/%3A/gi, ':').\n    replace(/%24/g, '$').\n    replace(/%2C/gi, ',').\n    replace(/%20/g, '+').\n    replace(/%5B/gi, '[').\n    replace(/%5D/gi, ']');\n}\n\n/**\n * Build a URL by appending params to the end\n *\n * @param {string} url The base of the url (e.g., http://www.google.com)\n * @param {object} [params] The params to be appended\n * @param {?object} options\n *\n * @returns {string} The formatted url\n */\nfunction buildURL(url, params, options) {\n  /*eslint no-param-reassign:0*/\n  if (!params) {\n    return url;\n  }\n  \n  const _encode = options && options.encode || encode;\n\n  const serializeFn = options && options.serialize;\n\n  let serializedParams;\n\n  if (serializeFn) {\n    serializedParams = serializeFn(params, options);\n  } else {\n    serializedParams = utils.isURLSearchParams(params) ?\n      params.toString() :\n      new AxiosURLSearchParams(params, options).toString(_encode);\n  }\n\n  if (serializedParams) {\n    const hashmarkIndex = url.indexOf(\"#\");\n\n    if (hashmarkIndex !== -1) {\n      url = url.slice(0, hashmarkIndex);\n    }\n    url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;\n  }\n\n  return url;\n}\n\nclass InterceptorManager {\n  constructor() {\n    this.handlers = [];\n  }\n\n  /**\n   * Add a new interceptor to the stack\n   *\n   * @param {Function} fulfilled The function to handle `then` for a `Promise`\n   * @param {Function} rejected The function to handle `reject` for a `Promise`\n   *\n   * @return {Number} An ID used to remove interceptor later\n   */\n  use(fulfilled, rejected, options) {\n    this.handlers.push({\n      fulfilled,\n      rejected,\n      synchronous: options ? options.synchronous : false,\n      runWhen: options ? options.runWhen : null\n    });\n    return this.handlers.length - 1;\n  }\n\n  /**\n   * Remove an interceptor from the stack\n   *\n   * @param {Number} id The ID that was returned by `use`\n   *\n   * @returns {Boolean} `true` if the interceptor was removed, `false` otherwise\n   */\n  eject(id) {\n    if (this.handlers[id]) {\n      this.handlers[id] = null;\n    }\n  }\n\n  /**\n   * Clear all interceptors from the stack\n   *\n   * @returns {void}\n   */\n  clear() {\n    if (this.handlers) {\n      this.handlers = [];\n    }\n  }\n\n  /**\n   * Iterate over all the registered interceptors\n   *\n   * This method is particularly useful for skipping over any\n   * interceptors that may have become `null` calling `eject`.\n   *\n   * @param {Function} fn The function to call for each interceptor\n   *\n   * @returns {void}\n   */\n  forEach(fn) {\n    utils.forEach(this.handlers, function forEachHandler(h) {\n      if (h !== null) {\n        fn(h);\n      }\n    });\n  }\n}\n\nvar InterceptorManager$1 = InterceptorManager;\n\nvar transitionalDefaults = {\n  silentJSONParsing: true,\n  forcedJSONParsing: true,\n  clarifyTimeoutError: false\n};\n\nvar URLSearchParams$1 = typeof URLSearchParams !== 'undefined' ? URLSearchParams : AxiosURLSearchParams;\n\nvar FormData$1 = typeof FormData !== 'undefined' ? FormData : null;\n\nvar Blob$1 = typeof Blob !== 'undefined' ? Blob : null;\n\n/**\n * Determine if we're running in a standard browser environment\n *\n * This allows axios to run in a web worker, and react-native.\n * Both environments support XMLHttpRequest, but not fully standard globals.\n *\n * web workers:\n *  typeof window -> undefined\n *  typeof document -> undefined\n *\n * react-native:\n *  navigator.product -> 'ReactNative'\n * nativescript\n *  navigator.product -> 'NativeScript' or 'NS'\n *\n * @returns {boolean}\n */\nconst isStandardBrowserEnv = (() => {\n  let product;\n  if (typeof navigator !== 'undefined' && (\n    (product = navigator.product) === 'ReactNative' ||\n    product === 'NativeScript' ||\n    product === 'NS')\n  ) {\n    return false;\n  }\n\n  return typeof window !== 'undefined' && typeof document !== 'undefined';\n})();\n\n/**\n * Determine if we're running in a standard browser webWorker environment\n *\n * Although the `isStandardBrowserEnv` method indicates that\n * `allows axios to run in a web worker`, the WebWorker will still be\n * filtered out due to its judgment standard\n * `typeof window !== 'undefined' && typeof document !== 'undefined'`.\n * This leads to a problem when axios post `FormData` in webWorker\n */\n const isStandardBrowserWebWorkerEnv = (() => {\n  return (\n    typeof WorkerGlobalScope !== 'undefined' &&\n    // eslint-disable-next-line no-undef\n    self instanceof WorkerGlobalScope &&\n    typeof self.importScripts === 'function'\n  );\n})();\n\n\nvar platform = {\n  isBrowser: true,\n  classes: {\n    URLSearchParams: URLSearchParams$1,\n    FormData: FormData$1,\n    Blob: Blob$1\n  },\n  isStandardBrowserEnv,\n  isStandardBrowserWebWorkerEnv,\n  protocols: ['http', 'https', 'file', 'blob', 'url', 'data']\n};\n\nfunction toURLEncodedForm(data, options) {\n  return toFormData(data, new platform.classes.URLSearchParams(), Object.assign({\n    visitor: function(value, key, path, helpers) {\n      if (platform.isNode && utils.isBuffer(value)) {\n        this.append(key, value.toString('base64'));\n        return false;\n      }\n\n      return helpers.defaultVisitor.apply(this, arguments);\n    }\n  }, options));\n}\n\n/**\n * It takes a string like `foo[x][y][z]` and returns an array like `['foo', 'x', 'y', 'z']\n *\n * @param {string} name - The name of the property to get.\n *\n * @returns An array of strings.\n */\nfunction parsePropPath(name) {\n  // foo[x][y][z]\n  // foo.x.y.z\n  // foo-x-y-z\n  // foo x y z\n  return utils.matchAll(/\\w+|\\[(\\w*)]/g, name).map(match => {\n    return match[0] === '[]' ? '' : match[1] || match[0];\n  });\n}\n\n/**\n * Convert an array to an object.\n *\n * @param {Array<any>} arr - The array to convert to an object.\n *\n * @returns An object with the same keys and values as the array.\n */\nfunction arrayToObject(arr) {\n  const obj = {};\n  const keys = Object.keys(arr);\n  let i;\n  const len = keys.length;\n  let key;\n  for (i = 0; i < len; i++) {\n    key = keys[i];\n    obj[key] = arr[key];\n  }\n  return obj;\n}\n\n/**\n * It takes a FormData object and returns a JavaScript object\n *\n * @param {string} formData The FormData object to convert to JSON.\n *\n * @returns {Object<string, any> | null} The converted object.\n */\nfunction formDataToJSON(formData) {\n  function buildPath(path, value, target, index) {\n    let name = path[index++];\n    const isNumericKey = Number.isFinite(+name);\n    const isLast = index >= path.length;\n    name = !name && utils.isArray(target) ? target.length : name;\n\n    if (isLast) {\n      if (utils.hasOwnProp(target, name)) {\n        target[name] = [target[name], value];\n      } else {\n        target[name] = value;\n      }\n\n      return !isNumericKey;\n    }\n\n    if (!target[name] || !utils.isObject(target[name])) {\n      target[name] = [];\n    }\n\n    const result = buildPath(path, value, target[name], index);\n\n    if (result && utils.isArray(target[name])) {\n      target[name] = arrayToObject(target[name]);\n    }\n\n    return !isNumericKey;\n  }\n\n  if (utils.isFormData(formData) && utils.isFunction(formData.entries)) {\n    const obj = {};\n\n    utils.forEachEntry(formData, (name, value) => {\n      buildPath(parsePropPath(name), value, obj, 0);\n    });\n\n    return obj;\n  }\n\n  return null;\n}\n\nconst DEFAULT_CONTENT_TYPE = {\n  'Content-Type': undefined\n};\n\n/**\n * It takes a string, tries to parse it, and if it fails, it returns the stringified version\n * of the input\n *\n * @param {any} rawValue - The value to be stringified.\n * @param {Function} parser - A function that parses a string into a JavaScript object.\n * @param {Function} encoder - A function that takes a value and returns a string.\n *\n * @returns {string} A stringified version of the rawValue.\n */\nfunction stringifySafely(rawValue, parser, encoder) {\n  if (utils.isString(rawValue)) {\n    try {\n      (parser || JSON.parse)(rawValue);\n      return utils.trim(rawValue);\n    } catch (e) {\n      if (e.name !== 'SyntaxError') {\n        throw e;\n      }\n    }\n  }\n\n  return (encoder || JSON.stringify)(rawValue);\n}\n\nconst defaults = {\n\n  transitional: transitionalDefaults,\n\n  adapter: ['xhr', 'http'],\n\n  transformRequest: [function transformRequest(data, headers) {\n    const contentType = headers.getContentType() || '';\n    const hasJSONContentType = contentType.indexOf('application/json') > -1;\n    const isObjectPayload = utils.isObject(data);\n\n    if (isObjectPayload && utils.isHTMLForm(data)) {\n      data = new FormData(data);\n    }\n\n    const isFormData = utils.isFormData(data);\n\n    if (isFormData) {\n      if (!hasJSONContentType) {\n        return data;\n      }\n      return hasJSONContentType ? JSON.stringify(formDataToJSON(data)) : data;\n    }\n\n    if (utils.isArrayBuffer(data) ||\n      utils.isBuffer(data) ||\n      utils.isStream(data) ||\n      utils.isFile(data) ||\n      utils.isBlob(data)\n    ) {\n      return data;\n    }\n    if (utils.isArrayBufferView(data)) {\n      return data.buffer;\n    }\n    if (utils.isURLSearchParams(data)) {\n      headers.setContentType('application/x-www-form-urlencoded;charset=utf-8', false);\n      return data.toString();\n    }\n\n    let isFileList;\n\n    if (isObjectPayload) {\n      if (contentType.indexOf('application/x-www-form-urlencoded') > -1) {\n        return toURLEncodedForm(data, this.formSerializer).toString();\n      }\n\n      if ((isFileList = utils.isFileList(data)) || contentType.indexOf('multipart/form-data') > -1) {\n        const _FormData = this.env && this.env.FormData;\n\n        return toFormData(\n          isFileList ? {'files[]': data} : data,\n          _FormData && new _FormData(),\n          this.formSerializer\n        );\n      }\n    }\n\n    if (isObjectPayload || hasJSONContentType ) {\n      headers.setContentType('application/json', false);\n      return stringifySafely(data);\n    }\n\n    return data;\n  }],\n\n  transformResponse: [function transformResponse(data) {\n    const transitional = this.transitional || defaults.transitional;\n    const forcedJSONParsing = transitional && transitional.forcedJSONParsing;\n    const JSONRequested = this.responseType === 'json';\n\n    if (data && utils.isString(data) && ((forcedJSONParsing && !this.responseType) || JSONRequested)) {\n      const silentJSONParsing = transitional && transitional.silentJSONParsing;\n      const strictJSONParsing = !silentJSONParsing && JSONRequested;\n\n      try {\n        return JSON.parse(data);\n      } catch (e) {\n        if (strictJSONParsing) {\n          if (e.name === 'SyntaxError') {\n            throw AxiosError.from(e, AxiosError.ERR_BAD_RESPONSE, this, null, this.response);\n          }\n          throw e;\n        }\n      }\n    }\n\n    return data;\n  }],\n\n  /**\n   * A timeout in milliseconds to abort a request. If set to 0 (default) a\n   * timeout is not created.\n   */\n  timeout: 0,\n\n  xsrfCookieName: 'XSRF-TOKEN',\n  xsrfHeaderName: 'X-XSRF-TOKEN',\n\n  maxContentLength: -1,\n  maxBodyLength: -1,\n\n  env: {\n    FormData: platform.classes.FormData,\n    Blob: platform.classes.Blob\n  },\n\n  validateStatus: function validateStatus(status) {\n    return status >= 200 && status < 300;\n  },\n\n  headers: {\n    common: {\n      'Accept': 'application/json, text/plain, */*'\n    }\n  }\n};\n\nutils.forEach(['delete', 'get', 'head'], function forEachMethodNoData(method) {\n  defaults.headers[method] = {};\n});\n\nutils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {\n  defaults.headers[method] = utils.merge(DEFAULT_CONTENT_TYPE);\n});\n\nvar defaults$1 = defaults;\n\n// RawAxiosHeaders whose duplicates are ignored by node\n// c.f. https://nodejs.org/api/http.html#http_message_headers\nconst ignoreDuplicateOf = utils.toObjectSet([\n  'age', 'authorization', 'content-length', 'content-type', 'etag',\n  'expires', 'from', 'host', 'if-modified-since', 'if-unmodified-since',\n  'last-modified', 'location', 'max-forwards', 'proxy-authorization',\n  'referer', 'retry-after', 'user-agent'\n]);\n\n/**\n * Parse headers into an object\n *\n * ```\n * Date: Wed, 27 Aug 2014 08:58:49 GMT\n * Content-Type: application/json\n * Connection: keep-alive\n * Transfer-Encoding: chunked\n * ```\n *\n * @param {String} rawHeaders Headers needing to be parsed\n *\n * @returns {Object} Headers parsed into an object\n */\nvar parseHeaders = rawHeaders => {\n  const parsed = {};\n  let key;\n  let val;\n  let i;\n\n  rawHeaders && rawHeaders.split('\\n').forEach(function parser(line) {\n    i = line.indexOf(':');\n    key = line.substring(0, i).trim().toLowerCase();\n    val = line.substring(i + 1).trim();\n\n    if (!key || (parsed[key] && ignoreDuplicateOf[key])) {\n      return;\n    }\n\n    if (key === 'set-cookie') {\n      if (parsed[key]) {\n        parsed[key].push(val);\n      } else {\n        parsed[key] = [val];\n      }\n    } else {\n      parsed[key] = parsed[key] ? parsed[key] + ', ' + val : val;\n    }\n  });\n\n  return parsed;\n};\n\nconst $internals = Symbol('internals');\n\nfunction normalizeHeader(header) {\n  return header && String(header).trim().toLowerCase();\n}\n\nfunction normalizeValue(value) {\n  if (value === false || value == null) {\n    return value;\n  }\n\n  return utils.isArray(value) ? value.map(normalizeValue) : String(value);\n}\n\nfunction parseTokens(str) {\n  const tokens = Object.create(null);\n  const tokensRE = /([^\\s,;=]+)\\s*(?:=\\s*([^,;]+))?/g;\n  let match;\n\n  while ((match = tokensRE.exec(str))) {\n    tokens[match[1]] = match[2];\n  }\n\n  return tokens;\n}\n\nconst isValidHeaderName = (str) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());\n\nfunction matchHeaderValue(context, value, header, filter, isHeaderNameFilter) {\n  if (utils.isFunction(filter)) {\n    return filter.call(this, value, header);\n  }\n\n  if (isHeaderNameFilter) {\n    value = header;\n  }\n\n  if (!utils.isString(value)) return;\n\n  if (utils.isString(filter)) {\n    return value.indexOf(filter) !== -1;\n  }\n\n  if (utils.isRegExp(filter)) {\n    return filter.test(value);\n  }\n}\n\nfunction formatHeader(header) {\n  return header.trim()\n    .toLowerCase().replace(/([a-z\\d])(\\w*)/g, (w, char, str) => {\n      return char.toUpperCase() + str;\n    });\n}\n\nfunction buildAccessors(obj, header) {\n  const accessorName = utils.toCamelCase(' ' + header);\n\n  ['get', 'set', 'has'].forEach(methodName => {\n    Object.defineProperty(obj, methodName + accessorName, {\n      value: function(arg1, arg2, arg3) {\n        return this[methodName].call(this, header, arg1, arg2, arg3);\n      },\n      configurable: true\n    });\n  });\n}\n\nclass AxiosHeaders {\n  constructor(headers) {\n    headers && this.set(headers);\n  }\n\n  set(header, valueOrRewrite, rewrite) {\n    const self = this;\n\n    function setHeader(_value, _header, _rewrite) {\n      const lHeader = normalizeHeader(_header);\n\n      if (!lHeader) {\n        throw new Error('header name must be a non-empty string');\n      }\n\n      const key = utils.findKey(self, lHeader);\n\n      if(!key || self[key] === undefined || _rewrite === true || (_rewrite === undefined && self[key] !== false)) {\n        self[key || _header] = normalizeValue(_value);\n      }\n    }\n\n    const setHeaders = (headers, _rewrite) =>\n      utils.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));\n\n    if (utils.isPlainObject(header) || header instanceof this.constructor) {\n      setHeaders(header, valueOrRewrite);\n    } else if(utils.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {\n      setHeaders(parseHeaders(header), valueOrRewrite);\n    } else {\n      header != null && setHeader(valueOrRewrite, header, rewrite);\n    }\n\n    return this;\n  }\n\n  get(header, parser) {\n    header = normalizeHeader(header);\n\n    if (header) {\n      const key = utils.findKey(this, header);\n\n      if (key) {\n        const value = this[key];\n\n        if (!parser) {\n          return value;\n        }\n\n        if (parser === true) {\n          return parseTokens(value);\n        }\n\n        if (utils.isFunction(parser)) {\n          return parser.call(this, value, key);\n        }\n\n        if (utils.isRegExp(parser)) {\n          return parser.exec(value);\n        }\n\n        throw new TypeError('parser must be boolean|regexp|function');\n      }\n    }\n  }\n\n  has(header, matcher) {\n    header = normalizeHeader(header);\n\n    if (header) {\n      const key = utils.findKey(this, header);\n\n      return !!(key && this[key] !== undefined && (!matcher || matchHeaderValue(this, this[key], key, matcher)));\n    }\n\n    return false;\n  }\n\n  delete(header, matcher) {\n    const self = this;\n    let deleted = false;\n\n    function deleteHeader(_header) {\n      _header = normalizeHeader(_header);\n\n      if (_header) {\n        const key = utils.findKey(self, _header);\n\n        if (key && (!matcher || matchHeaderValue(self, self[key], key, matcher))) {\n          delete self[key];\n\n          deleted = true;\n        }\n      }\n    }\n\n    if (utils.isArray(header)) {\n      header.forEach(deleteHeader);\n    } else {\n      deleteHeader(header);\n    }\n\n    return deleted;\n  }\n\n  clear(matcher) {\n    const keys = Object.keys(this);\n    let i = keys.length;\n    let deleted = false;\n\n    while (i--) {\n      const key = keys[i];\n      if(!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {\n        delete this[key];\n        deleted = true;\n      }\n    }\n\n    return deleted;\n  }\n\n  normalize(format) {\n    const self = this;\n    const headers = {};\n\n    utils.forEach(this, (value, header) => {\n      const key = utils.findKey(headers, header);\n\n      if (key) {\n        self[key] = normalizeValue(value);\n        delete self[header];\n        return;\n      }\n\n      const normalized = format ? formatHeader(header) : String(header).trim();\n\n      if (normalized !== header) {\n        delete self[header];\n      }\n\n      self[normalized] = normalizeValue(value);\n\n      headers[normalized] = true;\n    });\n\n    return this;\n  }\n\n  concat(...targets) {\n    return this.constructor.concat(this, ...targets);\n  }\n\n  toJSON(asStrings) {\n    const obj = Object.create(null);\n\n    utils.forEach(this, (value, header) => {\n      value != null && value !== false && (obj[header] = asStrings && utils.isArray(value) ? value.join(', ') : value);\n    });\n\n    return obj;\n  }\n\n  [Symbol.iterator]() {\n    return Object.entries(this.toJSON())[Symbol.iterator]();\n  }\n\n  toString() {\n    return Object.entries(this.toJSON()).map(([header, value]) => header + ': ' + value).join('\\n');\n  }\n\n  get [Symbol.toStringTag]() {\n    return 'AxiosHeaders';\n  }\n\n  static from(thing) {\n    return thing instanceof this ? thing : new this(thing);\n  }\n\n  static concat(first, ...targets) {\n    const computed = new this(first);\n\n    targets.forEach((target) => computed.set(target));\n\n    return computed;\n  }\n\n  static accessor(header) {\n    const internals = this[$internals] = (this[$internals] = {\n      accessors: {}\n    });\n\n    const accessors = internals.accessors;\n    const prototype = this.prototype;\n\n    function defineAccessor(_header) {\n      const lHeader = normalizeHeader(_header);\n\n      if (!accessors[lHeader]) {\n        buildAccessors(prototype, _header);\n        accessors[lHeader] = true;\n      }\n    }\n\n    utils.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);\n\n    return this;\n  }\n}\n\nAxiosHeaders.accessor(['Content-Type', 'Content-Length', 'Accept', 'Accept-Encoding', 'User-Agent', 'Authorization']);\n\nutils.freezeMethods(AxiosHeaders.prototype);\nutils.freezeMethods(AxiosHeaders);\n\nvar AxiosHeaders$1 = AxiosHeaders;\n\n/**\n * Transform the data for a request or a response\n *\n * @param {Array|Function} fns A single function or Array of functions\n * @param {?Object} response The response object\n *\n * @returns {*} The resulting transformed data\n */\nfunction transformData(fns, response) {\n  const config = this || defaults$1;\n  const context = response || config;\n  const headers = AxiosHeaders$1.from(context.headers);\n  let data = context.data;\n\n  utils.forEach(fns, function transform(fn) {\n    data = fn.call(config, data, headers.normalize(), response ? response.status : undefined);\n  });\n\n  headers.normalize();\n\n  return data;\n}\n\nfunction isCancel(value) {\n  return !!(value && value.__CANCEL__);\n}\n\n/**\n * A `CanceledError` is an object that is thrown when an operation is canceled.\n *\n * @param {string=} message The message.\n * @param {Object=} config The config.\n * @param {Object=} request The request.\n *\n * @returns {CanceledError} The created error.\n */\nfunction CanceledError(message, config, request) {\n  // eslint-disable-next-line no-eq-null,eqeqeq\n  AxiosError.call(this, message == null ? 'canceled' : message, AxiosError.ERR_CANCELED, config, request);\n  this.name = 'CanceledError';\n}\n\nutils.inherits(CanceledError, AxiosError, {\n  __CANCEL__: true\n});\n\n/**\n * Resolve or reject a Promise based on response status.\n *\n * @param {Function} resolve A function that resolves the promise.\n * @param {Function} reject A function that rejects the promise.\n * @param {object} response The response.\n *\n * @returns {object} The response.\n */\nfunction settle(resolve, reject, response) {\n  const validateStatus = response.config.validateStatus;\n  if (!response.status || !validateStatus || validateStatus(response.status)) {\n    resolve(response);\n  } else {\n    reject(new AxiosError(\n      'Request failed with status code ' + response.status,\n      [AxiosError.ERR_BAD_REQUEST, AxiosError.ERR_BAD_RESPONSE][Math.floor(response.status / 100) - 4],\n      response.config,\n      response.request,\n      response\n    ));\n  }\n}\n\nvar cookies = platform.isStandardBrowserEnv ?\n\n// Standard browser envs support document.cookie\n  (function standardBrowserEnv() {\n    return {\n      write: function write(name, value, expires, path, domain, secure) {\n        const cookie = [];\n        cookie.push(name + '=' + encodeURIComponent(value));\n\n        if (utils.isNumber(expires)) {\n          cookie.push('expires=' + new Date(expires).toGMTString());\n        }\n\n        if (utils.isString(path)) {\n          cookie.push('path=' + path);\n        }\n\n        if (utils.isString(domain)) {\n          cookie.push('domain=' + domain);\n        }\n\n        if (secure === true) {\n          cookie.push('secure');\n        }\n\n        document.cookie = cookie.join('; ');\n      },\n\n      read: function read(name) {\n        const match = document.cookie.match(new RegExp('(^|;\\\\s*)(' + name + ')=([^;]*)'));\n        return (match ? decodeURIComponent(match[3]) : null);\n      },\n\n      remove: function remove(name) {\n        this.write(name, '', Date.now() - 86400000);\n      }\n    };\n  })() :\n\n// Non standard browser env (web workers, react-native) lack needed support.\n  (function nonStandardBrowserEnv() {\n    return {\n      write: function write() {},\n      read: function read() { return null; },\n      remove: function remove() {}\n    };\n  })();\n\n/**\n * Determines whether the specified URL is absolute\n *\n * @param {string} url The URL to test\n *\n * @returns {boolean} True if the specified URL is absolute, otherwise false\n */\nfunction isAbsoluteURL(url) {\n  // A URL is considered absolute if it begins with \"<scheme>://\" or \"//\" (protocol-relative URL).\n  // RFC 3986 defines scheme name as a sequence of characters beginning with a letter and followed\n  // by any combination of letters, digits, plus, period, or hyphen.\n  return /^([a-z][a-z\\d+\\-.]*:)?\\/\\//i.test(url);\n}\n\n/**\n * Creates a new URL by combining the specified URLs\n *\n * @param {string} baseURL The base URL\n * @param {string} relativeURL The relative URL\n *\n * @returns {string} The combined URL\n */\nfunction combineURLs(baseURL, relativeURL) {\n  return relativeURL\n    ? baseURL.replace(/\\/+$/, '') + '/' + relativeURL.replace(/^\\/+/, '')\n    : baseURL;\n}\n\n/**\n * Creates a new URL by combining the baseURL with the requestedURL,\n * only when the requestedURL is not already an absolute URL.\n * If the requestURL is absolute, this function returns the requestedURL untouched.\n *\n * @param {string} baseURL The base URL\n * @param {string} requestedURL Absolute or relative URL to combine\n *\n * @returns {string} The combined full path\n */\nfunction buildFullPath(baseURL, requestedURL) {\n  if (baseURL && !isAbsoluteURL(requestedURL)) {\n    return combineURLs(baseURL, requestedURL);\n  }\n  return requestedURL;\n}\n\nvar isURLSameOrigin = platform.isStandardBrowserEnv ?\n\n// Standard browser envs have full support of the APIs needed to test\n// whether the request URL is of the same origin as current location.\n  (function standardBrowserEnv() {\n    const msie = /(msie|trident)/i.test(navigator.userAgent);\n    const urlParsingNode = document.createElement('a');\n    let originURL;\n\n    /**\n    * Parse a URL to discover it's components\n    *\n    * @param {String} url The URL to be parsed\n    * @returns {Object}\n    */\n    function resolveURL(url) {\n      let href = url;\n\n      if (msie) {\n        // IE needs attribute set twice to normalize properties\n        urlParsingNode.setAttribute('href', href);\n        href = urlParsingNode.href;\n      }\n\n      urlParsingNode.setAttribute('href', href);\n\n      // urlParsingNode provides the UrlUtils interface - http://url.spec.whatwg.org/#urlutils\n      return {\n        href: urlParsingNode.href,\n        protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, '') : '',\n        host: urlParsingNode.host,\n        search: urlParsingNode.search ? urlParsingNode.search.replace(/^\\?/, '') : '',\n        hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, '') : '',\n        hostname: urlParsingNode.hostname,\n        port: urlParsingNode.port,\n        pathname: (urlParsingNode.pathname.charAt(0) === '/') ?\n          urlParsingNode.pathname :\n          '/' + urlParsingNode.pathname\n      };\n    }\n\n    originURL = resolveURL(window.location.href);\n\n    /**\n    * Determine if a URL shares the same origin as the current location\n    *\n    * @param {String} requestURL The URL to test\n    * @returns {boolean} True if URL shares the same origin, otherwise false\n    */\n    return function isURLSameOrigin(requestURL) {\n      const parsed = (utils.isString(requestURL)) ? resolveURL(requestURL) : requestURL;\n      return (parsed.protocol === originURL.protocol &&\n          parsed.host === originURL.host);\n    };\n  })() :\n\n  // Non standard browser envs (web workers, react-native) lack needed support.\n  (function nonStandardBrowserEnv() {\n    return function isURLSameOrigin() {\n      return true;\n    };\n  })();\n\nfunction parseProtocol(url) {\n  const match = /^([-+\\w]{1,25})(:?\\/\\/|:)/.exec(url);\n  return match && match[1] || '';\n}\n\n/**\n * Calculate data maxRate\n * @param {Number} [samplesCount= 10]\n * @param {Number} [min= 1000]\n * @returns {Function}\n */\nfunction speedometer(samplesCount, min) {\n  samplesCount = samplesCount || 10;\n  const bytes = new Array(samplesCount);\n  const timestamps = new Array(samplesCount);\n  let head = 0;\n  let tail = 0;\n  let firstSampleTS;\n\n  min = min !== undefined ? min : 1000;\n\n  return function push(chunkLength) {\n    const now = Date.now();\n\n    const startedAt = timestamps[tail];\n\n    if (!firstSampleTS) {\n      firstSampleTS = now;\n    }\n\n    bytes[head] = chunkLength;\n    timestamps[head] = now;\n\n    let i = tail;\n    let bytesCount = 0;\n\n    while (i !== head) {\n      bytesCount += bytes[i++];\n      i = i % samplesCount;\n    }\n\n    head = (head + 1) % samplesCount;\n\n    if (head === tail) {\n      tail = (tail + 1) % samplesCount;\n    }\n\n    if (now - firstSampleTS < min) {\n      return;\n    }\n\n    const passed = startedAt && now - startedAt;\n\n    return passed ? Math.round(bytesCount * 1000 / passed) : undefined;\n  };\n}\n\nfunction progressEventReducer(listener, isDownloadStream) {\n  let bytesNotified = 0;\n  const _speedometer = speedometer(50, 250);\n\n  return e => {\n    const loaded = e.loaded;\n    const total = e.lengthComputable ? e.total : undefined;\n    const progressBytes = loaded - bytesNotified;\n    const rate = _speedometer(progressBytes);\n    const inRange = loaded <= total;\n\n    bytesNotified = loaded;\n\n    const data = {\n      loaded,\n      total,\n      progress: total ? (loaded / total) : undefined,\n      bytes: progressBytes,\n      rate: rate ? rate : undefined,\n      estimated: rate && total && inRange ? (total - loaded) / rate : undefined,\n      event: e\n    };\n\n    data[isDownloadStream ? 'download' : 'upload'] = true;\n\n    listener(data);\n  };\n}\n\nconst isXHRAdapterSupported = typeof XMLHttpRequest !== 'undefined';\n\nvar xhrAdapter = isXHRAdapterSupported && function (config) {\n  return new Promise(function dispatchXhrRequest(resolve, reject) {\n    let requestData = config.data;\n    const requestHeaders = AxiosHeaders$1.from(config.headers).normalize();\n    const responseType = config.responseType;\n    let onCanceled;\n    function done() {\n      if (config.cancelToken) {\n        config.cancelToken.unsubscribe(onCanceled);\n      }\n\n      if (config.signal) {\n        config.signal.removeEventListener('abort', onCanceled);\n      }\n    }\n\n    if (utils.isFormData(requestData)) {\n      if (platform.isStandardBrowserEnv || platform.isStandardBrowserWebWorkerEnv) {\n        requestHeaders.setContentType(false); // Let the browser set it\n      } else {\n        requestHeaders.setContentType('multipart/form-data;', false); // mobile/desktop app frameworks\n      }\n    }\n\n    let request = new XMLHttpRequest();\n\n    // HTTP basic authentication\n    if (config.auth) {\n      const username = config.auth.username || '';\n      const password = config.auth.password ? unescape(encodeURIComponent(config.auth.password)) : '';\n      requestHeaders.set('Authorization', 'Basic ' + btoa(username + ':' + password));\n    }\n\n    const fullPath = buildFullPath(config.baseURL, config.url);\n\n    request.open(config.method.toUpperCase(), buildURL(fullPath, config.params, config.paramsSerializer), true);\n\n    // Set the request timeout in MS\n    request.timeout = config.timeout;\n\n    function onloadend() {\n      if (!request) {\n        return;\n      }\n      // Prepare the response\n      const responseHeaders = AxiosHeaders$1.from(\n        'getAllResponseHeaders' in request && request.getAllResponseHeaders()\n      );\n      const responseData = !responseType || responseType === 'text' || responseType === 'json' ?\n        request.responseText : request.response;\n      const response = {\n        data: responseData,\n        status: request.status,\n        statusText: request.statusText,\n        headers: responseHeaders,\n        config,\n        request\n      };\n\n      settle(function _resolve(value) {\n        resolve(value);\n        done();\n      }, function _reject(err) {\n        reject(err);\n        done();\n      }, response);\n\n      // Clean up request\n      request = null;\n    }\n\n    if ('onloadend' in request) {\n      // Use onloadend if available\n      request.onloadend = onloadend;\n    } else {\n      // Listen for ready state to emulate onloadend\n      request.onreadystatechange = function handleLoad() {\n        if (!request || request.readyState !== 4) {\n          return;\n        }\n\n        // The request errored out and we didn't get a response, this will be\n        // handled by onerror instead\n        // With one exception: request that using file: protocol, most browsers\n        // will return status as 0 even though it's a successful request\n        if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf('file:') === 0)) {\n          return;\n        }\n        // readystate handler is calling before onerror or ontimeout handlers,\n        // so we should call onloadend on the next 'tick'\n        setTimeout(onloadend);\n      };\n    }\n\n    // Handle browser request cancellation (as opposed to a manual cancellation)\n    request.onabort = function handleAbort() {\n      if (!request) {\n        return;\n      }\n\n      reject(new AxiosError('Request aborted', AxiosError.ECONNABORTED, config, request));\n\n      // Clean up request\n      request = null;\n    };\n\n    // Handle low level network errors\n    request.onerror = function handleError() {\n      // Real errors are hidden from us by the browser\n      // onerror should only fire if it's a network error\n      reject(new AxiosError('Network Error', AxiosError.ERR_NETWORK, config, request));\n\n      // Clean up request\n      request = null;\n    };\n\n    // Handle timeout\n    request.ontimeout = function handleTimeout() {\n      let timeoutErrorMessage = config.timeout ? 'timeout of ' + config.timeout + 'ms exceeded' : 'timeout exceeded';\n      const transitional = config.transitional || transitionalDefaults;\n      if (config.timeoutErrorMessage) {\n        timeoutErrorMessage = config.timeoutErrorMessage;\n      }\n      reject(new AxiosError(\n        timeoutErrorMessage,\n        transitional.clarifyTimeoutError ? AxiosError.ETIMEDOUT : AxiosError.ECONNABORTED,\n        config,\n        request));\n\n      // Clean up request\n      request = null;\n    };\n\n    // Add xsrf header\n    // This is only done if running in a standard browser environment.\n    // Specifically not if we're in a web worker, or react-native.\n    if (platform.isStandardBrowserEnv) {\n      // Add xsrf header\n      const xsrfValue = (config.withCredentials || isURLSameOrigin(fullPath))\n        && config.xsrfCookieName && cookies.read(config.xsrfCookieName);\n\n      if (xsrfValue) {\n        requestHeaders.set(config.xsrfHeaderName, xsrfValue);\n      }\n    }\n\n    // Remove Content-Type if data is undefined\n    requestData === undefined && requestHeaders.setContentType(null);\n\n    // Add headers to the request\n    if ('setRequestHeader' in request) {\n      utils.forEach(requestHeaders.toJSON(), function setRequestHeader(val, key) {\n        request.setRequestHeader(key, val);\n      });\n    }\n\n    // Add withCredentials to request if needed\n    if (!utils.isUndefined(config.withCredentials)) {\n      request.withCredentials = !!config.withCredentials;\n    }\n\n    // Add responseType to request if needed\n    if (responseType && responseType !== 'json') {\n      request.responseType = config.responseType;\n    }\n\n    // Handle progress if needed\n    if (typeof config.onDownloadProgress === 'function') {\n      request.addEventListener('progress', progressEventReducer(config.onDownloadProgress, true));\n    }\n\n    // Not all browsers support upload events\n    if (typeof config.onUploadProgress === 'function' && request.upload) {\n      request.upload.addEventListener('progress', progressEventReducer(config.onUploadProgress));\n    }\n\n    if (config.cancelToken || config.signal) {\n      // Handle cancellation\n      // eslint-disable-next-line func-names\n      onCanceled = cancel => {\n        if (!request) {\n          return;\n        }\n        reject(!cancel || cancel.type ? new CanceledError(null, config, request) : cancel);\n        request.abort();\n        request = null;\n      };\n\n      config.cancelToken && config.cancelToken.subscribe(onCanceled);\n      if (config.signal) {\n        config.signal.aborted ? onCanceled() : config.signal.addEventListener('abort', onCanceled);\n      }\n    }\n\n    const protocol = parseProtocol(fullPath);\n\n    if (protocol && platform.protocols.indexOf(protocol) === -1) {\n      reject(new AxiosError('Unsupported protocol ' + protocol + ':', AxiosError.ERR_BAD_REQUEST, config));\n      return;\n    }\n\n\n    // Send the request\n    request.send(requestData || null);\n  });\n};\n\nconst knownAdapters = {\n  http: httpAdapter,\n  xhr: xhrAdapter\n};\n\nutils.forEach(knownAdapters, (fn, value) => {\n  if(fn) {\n    try {\n      Object.defineProperty(fn, 'name', {value});\n    } catch (e) {\n      // eslint-disable-next-line no-empty\n    }\n    Object.defineProperty(fn, 'adapterName', {value});\n  }\n});\n\nvar adapters = {\n  getAdapter: (adapters) => {\n    adapters = utils.isArray(adapters) ? adapters : [adapters];\n\n    const {length} = adapters;\n    let nameOrAdapter;\n    let adapter;\n\n    for (let i = 0; i < length; i++) {\n      nameOrAdapter = adapters[i];\n      if((adapter = utils.isString(nameOrAdapter) ? knownAdapters[nameOrAdapter.toLowerCase()] : nameOrAdapter)) {\n        break;\n      }\n    }\n\n    if (!adapter) {\n      if (adapter === false) {\n        throw new AxiosError(\n          `Adapter ${nameOrAdapter} is not supported by the environment`,\n          'ERR_NOT_SUPPORT'\n        );\n      }\n\n      throw new Error(\n        utils.hasOwnProp(knownAdapters, nameOrAdapter) ?\n          `Adapter '${nameOrAdapter}' is not available in the build` :\n          `Unknown adapter '${nameOrAdapter}'`\n      );\n    }\n\n    if (!utils.isFunction(adapter)) {\n      throw new TypeError('adapter is not a function');\n    }\n\n    return adapter;\n  },\n  adapters: knownAdapters\n};\n\n/**\n * Throws a `CanceledError` if cancellation has been requested.\n *\n * @param {Object} config The config that is to be used for the request\n *\n * @returns {void}\n */\nfunction throwIfCancellationRequested(config) {\n  if (config.cancelToken) {\n    config.cancelToken.throwIfRequested();\n  }\n\n  if (config.signal && config.signal.aborted) {\n    throw new CanceledError(null, config);\n  }\n}\n\n/**\n * Dispatch a request to the server using the configured adapter.\n *\n * @param {object} config The config that is to be used for the request\n *\n * @returns {Promise} The Promise to be fulfilled\n */\nfunction dispatchRequest(config) {\n  throwIfCancellationRequested(config);\n\n  config.headers = AxiosHeaders$1.from(config.headers);\n\n  // Transform request data\n  config.data = transformData.call(\n    config,\n    config.transformRequest\n  );\n\n  if (['post', 'put', 'patch'].indexOf(config.method) !== -1) {\n    config.headers.setContentType('application/x-www-form-urlencoded', false);\n  }\n\n  const adapter = adapters.getAdapter(config.adapter || defaults$1.adapter);\n\n  return adapter(config).then(function onAdapterResolution(response) {\n    throwIfCancellationRequested(config);\n\n    // Transform response data\n    response.data = transformData.call(\n      config,\n      config.transformResponse,\n      response\n    );\n\n    response.headers = AxiosHeaders$1.from(response.headers);\n\n    return response;\n  }, function onAdapterRejection(reason) {\n    if (!isCancel(reason)) {\n      throwIfCancellationRequested(config);\n\n      // Transform response data\n      if (reason && reason.response) {\n        reason.response.data = transformData.call(\n          config,\n          config.transformResponse,\n          reason.response\n        );\n        reason.response.headers = AxiosHeaders$1.from(reason.response.headers);\n      }\n    }\n\n    return Promise.reject(reason);\n  });\n}\n\nconst headersToObject = (thing) => thing instanceof AxiosHeaders$1 ? thing.toJSON() : thing;\n\n/**\n * Config-specific merge-function which creates a new config-object\n * by merging two configuration objects together.\n *\n * @param {Object} config1\n * @param {Object} config2\n *\n * @returns {Object} New object resulting from merging config2 to config1\n */\nfunction mergeConfig(config1, config2) {\n  // eslint-disable-next-line no-param-reassign\n  config2 = config2 || {};\n  const config = {};\n\n  function getMergedValue(target, source, caseless) {\n    if (utils.isPlainObject(target) && utils.isPlainObject(source)) {\n      return utils.merge.call({caseless}, target, source);\n    } else if (utils.isPlainObject(source)) {\n      return utils.merge({}, source);\n    } else if (utils.isArray(source)) {\n      return source.slice();\n    }\n    return source;\n  }\n\n  // eslint-disable-next-line consistent-return\n  function mergeDeepProperties(a, b, caseless) {\n    if (!utils.isUndefined(b)) {\n      return getMergedValue(a, b, caseless);\n    } else if (!utils.isUndefined(a)) {\n      return getMergedValue(undefined, a, caseless);\n    }\n  }\n\n  // eslint-disable-next-line consistent-return\n  function valueFromConfig2(a, b) {\n    if (!utils.isUndefined(b)) {\n      return getMergedValue(undefined, b);\n    }\n  }\n\n  // eslint-disable-next-line consistent-return\n  function defaultToConfig2(a, b) {\n    if (!utils.isUndefined(b)) {\n      return getMergedValue(undefined, b);\n    } else if (!utils.isUndefined(a)) {\n      return getMergedValue(undefined, a);\n    }\n  }\n\n  // eslint-disable-next-line consistent-return\n  function mergeDirectKeys(a, b, prop) {\n    if (prop in config2) {\n      return getMergedValue(a, b);\n    } else if (prop in config1) {\n      return getMergedValue(undefined, a);\n    }\n  }\n\n  const mergeMap = {\n    url: valueFromConfig2,\n    method: valueFromConfig2,\n    data: valueFromConfig2,\n    baseURL: defaultToConfig2,\n    transformRequest: defaultToConfig2,\n    transformResponse: defaultToConfig2,\n    paramsSerializer: defaultToConfig2,\n    timeout: defaultToConfig2,\n    timeoutMessage: defaultToConfig2,\n    withCredentials: defaultToConfig2,\n    adapter: defaultToConfig2,\n    responseType: defaultToConfig2,\n    xsrfCookieName: defaultToConfig2,\n    xsrfHeaderName: defaultToConfig2,\n    onUploadProgress: defaultToConfig2,\n    onDownloadProgress: defaultToConfig2,\n    decompress: defaultToConfig2,\n    maxContentLength: defaultToConfig2,\n    maxBodyLength: defaultToConfig2,\n    beforeRedirect: defaultToConfig2,\n    transport: defaultToConfig2,\n    httpAgent: defaultToConfig2,\n    httpsAgent: defaultToConfig2,\n    cancelToken: defaultToConfig2,\n    socketPath: defaultToConfig2,\n    responseEncoding: defaultToConfig2,\n    validateStatus: mergeDirectKeys,\n    headers: (a, b) => mergeDeepProperties(headersToObject(a), headersToObject(b), true)\n  };\n\n  utils.forEach(Object.keys(Object.assign({}, config1, config2)), function computeConfigValue(prop) {\n    const merge = mergeMap[prop] || mergeDeepProperties;\n    const configValue = merge(config1[prop], config2[prop], prop);\n    (utils.isUndefined(configValue) && merge !== mergeDirectKeys) || (config[prop] = configValue);\n  });\n\n  return config;\n}\n\nconst VERSION = \"1.4.0\";\n\nconst validators$1 = {};\n\n// eslint-disable-next-line func-names\n['object', 'boolean', 'number', 'function', 'string', 'symbol'].forEach((type, i) => {\n  validators$1[type] = function validator(thing) {\n    return typeof thing === type || 'a' + (i < 1 ? 'n ' : ' ') + type;\n  };\n});\n\nconst deprecatedWarnings = {};\n\n/**\n * Transitional option validator\n *\n * @param {function|boolean?} validator - set to false if the transitional option has been removed\n * @param {string?} version - deprecated version / removed since version\n * @param {string?} message - some message with additional info\n *\n * @returns {function}\n */\nvalidators$1.transitional = function transitional(validator, version, message) {\n  function formatMessage(opt, desc) {\n    return '[Axios v' + VERSION + '] Transitional option \\'' + opt + '\\'' + desc + (message ? '. ' + message : '');\n  }\n\n  // eslint-disable-next-line func-names\n  return (value, opt, opts) => {\n    if (validator === false) {\n      throw new AxiosError(\n        formatMessage(opt, ' has been removed' + (version ? ' in ' + version : '')),\n        AxiosError.ERR_DEPRECATED\n      );\n    }\n\n    if (version && !deprecatedWarnings[opt]) {\n      deprecatedWarnings[opt] = true;\n      // eslint-disable-next-line no-console\n      console.warn(\n        formatMessage(\n          opt,\n          ' has been deprecated since v' + version + ' and will be removed in the near future'\n        )\n      );\n    }\n\n    return validator ? validator(value, opt, opts) : true;\n  };\n};\n\n/**\n * Assert object's properties type\n *\n * @param {object} options\n * @param {object} schema\n * @param {boolean?} allowUnknown\n *\n * @returns {object}\n */\n\nfunction assertOptions(options, schema, allowUnknown) {\n  if (typeof options !== 'object') {\n    throw new AxiosError('options must be an object', AxiosError.ERR_BAD_OPTION_VALUE);\n  }\n  const keys = Object.keys(options);\n  let i = keys.length;\n  while (i-- > 0) {\n    const opt = keys[i];\n    const validator = schema[opt];\n    if (validator) {\n      const value = options[opt];\n      const result = value === undefined || validator(value, opt, options);\n      if (result !== true) {\n        throw new AxiosError('option ' + opt + ' must be ' + result, AxiosError.ERR_BAD_OPTION_VALUE);\n      }\n      continue;\n    }\n    if (allowUnknown !== true) {\n      throw new AxiosError('Unknown option ' + opt, AxiosError.ERR_BAD_OPTION);\n    }\n  }\n}\n\nvar validator = {\n  assertOptions,\n  validators: validators$1\n};\n\nconst validators = validator.validators;\n\n/**\n * Create a new instance of Axios\n *\n * @param {Object} instanceConfig The default config for the instance\n *\n * @return {Axios} A new instance of Axios\n */\nclass Axios {\n  constructor(instanceConfig) {\n    this.defaults = instanceConfig;\n    this.interceptors = {\n      request: new InterceptorManager$1(),\n      response: new InterceptorManager$1()\n    };\n  }\n\n  /**\n   * Dispatch a request\n   *\n   * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)\n   * @param {?Object} config\n   *\n   * @returns {Promise} The Promise to be fulfilled\n   */\n  request(configOrUrl, config) {\n    /*eslint no-param-reassign:0*/\n    // Allow for axios('example/url'[, config]) a la fetch API\n    if (typeof configOrUrl === 'string') {\n      config = config || {};\n      config.url = configOrUrl;\n    } else {\n      config = configOrUrl || {};\n    }\n\n    config = mergeConfig(this.defaults, config);\n\n    const {transitional, paramsSerializer, headers} = config;\n\n    if (transitional !== undefined) {\n      validator.assertOptions(transitional, {\n        silentJSONParsing: validators.transitional(validators.boolean),\n        forcedJSONParsing: validators.transitional(validators.boolean),\n        clarifyTimeoutError: validators.transitional(validators.boolean)\n      }, false);\n    }\n\n    if (paramsSerializer != null) {\n      if (utils.isFunction(paramsSerializer)) {\n        config.paramsSerializer = {\n          serialize: paramsSerializer\n        };\n      } else {\n        validator.assertOptions(paramsSerializer, {\n          encode: validators.function,\n          serialize: validators.function\n        }, true);\n      }\n    }\n\n    // Set config.method\n    config.method = (config.method || this.defaults.method || 'get').toLowerCase();\n\n    let contextHeaders;\n\n    // Flatten headers\n    contextHeaders = headers && utils.merge(\n      headers.common,\n      headers[config.method]\n    );\n\n    contextHeaders && utils.forEach(\n      ['delete', 'get', 'head', 'post', 'put', 'patch', 'common'],\n      (method) => {\n        delete headers[method];\n      }\n    );\n\n    config.headers = AxiosHeaders$1.concat(contextHeaders, headers);\n\n    // filter out skipped interceptors\n    const requestInterceptorChain = [];\n    let synchronousRequestInterceptors = true;\n    this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {\n      if (typeof interceptor.runWhen === 'function' && interceptor.runWhen(config) === false) {\n        return;\n      }\n\n      synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;\n\n      requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);\n    });\n\n    const responseInterceptorChain = [];\n    this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {\n      responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);\n    });\n\n    let promise;\n    let i = 0;\n    let len;\n\n    if (!synchronousRequestInterceptors) {\n      const chain = [dispatchRequest.bind(this), undefined];\n      chain.unshift.apply(chain, requestInterceptorChain);\n      chain.push.apply(chain, responseInterceptorChain);\n      len = chain.length;\n\n      promise = Promise.resolve(config);\n\n      while (i < len) {\n        promise = promise.then(chain[i++], chain[i++]);\n      }\n\n      return promise;\n    }\n\n    len = requestInterceptorChain.length;\n\n    let newConfig = config;\n\n    i = 0;\n\n    while (i < len) {\n      const onFulfilled = requestInterceptorChain[i++];\n      const onRejected = requestInterceptorChain[i++];\n      try {\n        newConfig = onFulfilled(newConfig);\n      } catch (error) {\n        onRejected.call(this, error);\n        break;\n      }\n    }\n\n    try {\n      promise = dispatchRequest.call(this, newConfig);\n    } catch (error) {\n      return Promise.reject(error);\n    }\n\n    i = 0;\n    len = responseInterceptorChain.length;\n\n    while (i < len) {\n      promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);\n    }\n\n    return promise;\n  }\n\n  getUri(config) {\n    config = mergeConfig(this.defaults, config);\n    const fullPath = buildFullPath(config.baseURL, config.url);\n    return buildURL(fullPath, config.params, config.paramsSerializer);\n  }\n}\n\n// Provide aliases for supported request methods\nutils.forEach(['delete', 'get', 'head', 'options'], function forEachMethodNoData(method) {\n  /*eslint func-names:0*/\n  Axios.prototype[method] = function(url, config) {\n    return this.request(mergeConfig(config || {}, {\n      method,\n      url,\n      data: (config || {}).data\n    }));\n  };\n});\n\nutils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {\n  /*eslint func-names:0*/\n\n  function generateHTTPMethod(isForm) {\n    return function httpMethod(url, data, config) {\n      return this.request(mergeConfig(config || {}, {\n        method,\n        headers: isForm ? {\n          'Content-Type': 'multipart/form-data'\n        } : {},\n        url,\n        data\n      }));\n    };\n  }\n\n  Axios.prototype[method] = generateHTTPMethod();\n\n  Axios.prototype[method + 'Form'] = generateHTTPMethod(true);\n});\n\nvar Axios$1 = Axios;\n\n/**\n * A `CancelToken` is an object that can be used to request cancellation of an operation.\n *\n * @param {Function} executor The executor function.\n *\n * @returns {CancelToken}\n */\nclass CancelToken {\n  constructor(executor) {\n    if (typeof executor !== 'function') {\n      throw new TypeError('executor must be a function.');\n    }\n\n    let resolvePromise;\n\n    this.promise = new Promise(function promiseExecutor(resolve) {\n      resolvePromise = resolve;\n    });\n\n    const token = this;\n\n    // eslint-disable-next-line func-names\n    this.promise.then(cancel => {\n      if (!token._listeners) return;\n\n      let i = token._listeners.length;\n\n      while (i-- > 0) {\n        token._listeners[i](cancel);\n      }\n      token._listeners = null;\n    });\n\n    // eslint-disable-next-line func-names\n    this.promise.then = onfulfilled => {\n      let _resolve;\n      // eslint-disable-next-line func-names\n      const promise = new Promise(resolve => {\n        token.subscribe(resolve);\n        _resolve = resolve;\n      }).then(onfulfilled);\n\n      promise.cancel = function reject() {\n        token.unsubscribe(_resolve);\n      };\n\n      return promise;\n    };\n\n    executor(function cancel(message, config, request) {\n      if (token.reason) {\n        // Cancellation has already been requested\n        return;\n      }\n\n      token.reason = new CanceledError(message, config, request);\n      resolvePromise(token.reason);\n    });\n  }\n\n  /**\n   * Throws a `CanceledError` if cancellation has been requested.\n   */\n  throwIfRequested() {\n    if (this.reason) {\n      throw this.reason;\n    }\n  }\n\n  /**\n   * Subscribe to the cancel signal\n   */\n\n  subscribe(listener) {\n    if (this.reason) {\n      listener(this.reason);\n      return;\n    }\n\n    if (this._listeners) {\n      this._listeners.push(listener);\n    } else {\n      this._listeners = [listener];\n    }\n  }\n\n  /**\n   * Unsubscribe from the cancel signal\n   */\n\n  unsubscribe(listener) {\n    if (!this._listeners) {\n      return;\n    }\n    const index = this._listeners.indexOf(listener);\n    if (index !== -1) {\n      this._listeners.splice(index, 1);\n    }\n  }\n\n  /**\n   * Returns an object that contains a new `CancelToken` and a function that, when called,\n   * cancels the `CancelToken`.\n   */\n  static source() {\n    let cancel;\n    const token = new CancelToken(function executor(c) {\n      cancel = c;\n    });\n    return {\n      token,\n      cancel\n    };\n  }\n}\n\nvar CancelToken$1 = CancelToken;\n\n/**\n * Syntactic sugar for invoking a function and expanding an array for arguments.\n *\n * Common use case would be to use `Function.prototype.apply`.\n *\n *  ```js\n *  function f(x, y, z) {}\n *  var args = [1, 2, 3];\n *  f.apply(null, args);\n *  ```\n *\n * With `spread` this example can be re-written.\n *\n *  ```js\n *  spread(function(x, y, z) {})([1, 2, 3]);\n *  ```\n *\n * @param {Function} callback\n *\n * @returns {Function}\n */\nfunction spread(callback) {\n  return function wrap(arr) {\n    return callback.apply(null, arr);\n  };\n}\n\n/**\n * Determines whether the payload is an error thrown by Axios\n *\n * @param {*} payload The value to test\n *\n * @returns {boolean} True if the payload is an error thrown by Axios, otherwise false\n */\nfunction isAxiosError(payload) {\n  return utils.isObject(payload) && (payload.isAxiosError === true);\n}\n\nconst HttpStatusCode = {\n  Continue: 100,\n  SwitchingProtocols: 101,\n  Processing: 102,\n  EarlyHints: 103,\n  Ok: 200,\n  Created: 201,\n  Accepted: 202,\n  NonAuthoritativeInformation: 203,\n  NoContent: 204,\n  ResetContent: 205,\n  PartialContent: 206,\n  MultiStatus: 207,\n  AlreadyReported: 208,\n  ImUsed: 226,\n  MultipleChoices: 300,\n  MovedPermanently: 301,\n  Found: 302,\n  SeeOther: 303,\n  NotModified: 304,\n  UseProxy: 305,\n  Unused: 306,\n  TemporaryRedirect: 307,\n  PermanentRedirect: 308,\n  BadRequest: 400,\n  Unauthorized: 401,\n  PaymentRequired: 402,\n  Forbidden: 403,\n  NotFound: 404,\n  MethodNotAllowed: 405,\n  NotAcceptable: 406,\n  ProxyAuthenticationRequired: 407,\n  RequestTimeout: 408,\n  Conflict: 409,\n  Gone: 410,\n  LengthRequired: 411,\n  PreconditionFailed: 412,\n  PayloadTooLarge: 413,\n  UriTooLong: 414,\n  UnsupportedMediaType: 415,\n  RangeNotSatisfiable: 416,\n  ExpectationFailed: 417,\n  ImATeapot: 418,\n  MisdirectedRequest: 421,\n  UnprocessableEntity: 422,\n  Locked: 423,\n  FailedDependency: 424,\n  TooEarly: 425,\n  UpgradeRequired: 426,\n  PreconditionRequired: 428,\n  TooManyRequests: 429,\n  RequestHeaderFieldsTooLarge: 431,\n  UnavailableForLegalReasons: 451,\n  InternalServerError: 500,\n  NotImplemented: 501,\n  BadGateway: 502,\n  ServiceUnavailable: 503,\n  GatewayTimeout: 504,\n  HttpVersionNotSupported: 505,\n  VariantAlsoNegotiates: 506,\n  InsufficientStorage: 507,\n  LoopDetected: 508,\n  NotExtended: 510,\n  NetworkAuthenticationRequired: 511,\n};\n\nObject.entries(HttpStatusCode).forEach(([key, value]) => {\n  HttpStatusCode[value] = key;\n});\n\nvar HttpStatusCode$1 = HttpStatusCode;\n\n/**\n * Create an instance of Axios\n *\n * @param {Object} defaultConfig The default config for the instance\n *\n * @returns {Axios} A new instance of Axios\n */\nfunction createInstance(defaultConfig) {\n  const context = new Axios$1(defaultConfig);\n  const instance = bind(Axios$1.prototype.request, context);\n\n  // Copy axios.prototype to instance\n  utils.extend(instance, Axios$1.prototype, context, {allOwnKeys: true});\n\n  // Copy context to instance\n  utils.extend(instance, context, null, {allOwnKeys: true});\n\n  // Factory for creating new instances\n  instance.create = function create(instanceConfig) {\n    return createInstance(mergeConfig(defaultConfig, instanceConfig));\n  };\n\n  return instance;\n}\n\n// Create the default instance to be exported\nconst axios = createInstance(defaults$1);\n\n// Expose Axios class to allow class inheritance\naxios.Axios = Axios$1;\n\n// Expose Cancel & CancelToken\naxios.CanceledError = CanceledError;\naxios.CancelToken = CancelToken$1;\naxios.isCancel = isCancel;\naxios.VERSION = VERSION;\naxios.toFormData = toFormData;\n\n// Expose AxiosError class\naxios.AxiosError = AxiosError;\n\n// alias for CanceledError for backward compatibility\naxios.Cancel = axios.CanceledError;\n\n// Expose all/spread\naxios.all = function all(promises) {\n  return Promise.all(promises);\n};\n\naxios.spread = spread;\n\n// Expose isAxiosError\naxios.isAxiosError = isAxiosError;\n\n// Expose mergeConfig\naxios.mergeConfig = mergeConfig;\n\naxios.AxiosHeaders = AxiosHeaders$1;\n\naxios.formToJSON = thing => formDataToJSON(utils.isHTMLForm(thing) ? new FormData(thing) : thing);\n\naxios.HttpStatusCode = HttpStatusCode$1;\n\naxios.default = axios;\n\nmodule.exports = axios;\n//# sourceMappingURL=axios.cjs.map\n\n\n//# sourceURL=webpack:///./node_modules/axios/dist/browser/axios.cjs?")}},__webpack_module_cache__={};function __webpack_require__(n){var e=__webpack_module_cache__[n];if(void 0!==e)return e.exports;var t=__webpack_module_cache__[n]={exports:{}};return __webpack_modules__[n](t,t.exports,__webpack_require__),t.exports}__webpack_require__.g=function(){if("object"==typeof globalThis)return globalThis;try{return this||new Function("return this")()}catch(n){if("object"==typeof window)return window}}();var __webpack_exports__=__webpack_require__("./assets/js/show-ads.js")})();